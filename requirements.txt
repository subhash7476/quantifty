# Upstox Bot — core infrastructure dependencies
# Pinned to the versions validated during the F:\Nifty migration (2026-06-04).
# Derived from an AST scan of the actual imports across core/, app_facade/,
# flask_app/, scripts/ — not pipreqs guesswork.

# Data / storage
duckdb==1.5.2
pandas==3.0.2
numpy==2.4.4
pyarrow==23.0.1

# Web / UI
Flask==3.1.3

# Broker + market data (REST + WebSocket; no vendor SDK — direct HTTP/WS)
requests==2.33.1
websockets==15.0.1
protobuf==7.34.1          # Upstox MarketDataFeedV3 protobuf decoding

# Messaging / scheduling
pyzmq==27.1.0

# Modelling — the DayType live path unpickles scikit-learn objects at runtime
# (core/state/daytype_engine.py), and cluster_day_types.py needs scipy. These
# were undeclared while being load-bearing: audit Finding E,
# docs/reports/index_research/REGIME_DETECTION_SPEC_AUDIT_2026-09-09.md.
# A pickle is version-coupled to the library that wrote it, so these pins are
# part of the model contract, not a convenience.
scikit-learn==1.8.0
scipy==1.17.0
lightgbm==4.7.0

# Time
pytz==2026.2
