# CHANGELOG_PLATFORM.md

Append-only history of major platform milestones for `F:\Nifty`. Newest entries at the top. Record *what happened*; rationale and current status live in `docs/ARCHITECTURE_DECISIONS.md` and `docs/PROJECT_STATE.md`.

Format: `## YYYY-MM-DD — <milestone>` with a short factual description and source reference.

---

## 2026-06-15 — Planned #4 COMPLETE — F&O broker product/segment model (asset-class defaults; 538→541 passing)

`core/brokers/mapping/upstox.py` now emits the correct Upstox product code for every order without requiring an explicit `ci.product` field. **Root cause closed:** `CanonicalInstrument.product` is always `None` (the instrument master has no product column; `InstrumentResolver._build()` never sets it); the existing `_PRODUCT.get(instrument.product)` path returned `None`, and the `or "I"` fallback in `upstox_adapter.py` silently hardcoded MIS for every order including futures carry and overnight option selling. **Fix (one file):** `_ASSET_DEFAULT_PRODUCT` dict added to `upstox.py` (`AssetClass` import added); `to_broker()` resolves `product_intent = instrument.product or _ASSET_DEFAULT_PRODUCT.get(instrument.asset_class)` — explicit override wins (priority 1), asset-class default fills the gap (priority 2), adapter `or "I"` stays as unreachable safety net (priority 3). **Policy:** FUTURE→NRML→`"D"`, OPTION→NRML→`"D"`, EQUITY→CNC→`"D"`; INDEX non-tradable (blocked upstream). **Tests (+3):** `test_to_broker_default_product_by_asset_class` (characterizes that resolver returns `product=None`, confirms FUTURE/OPTION/EQUITY mapping-layer defaults); P7 end-to-end payload OPTION CI `product=None` → `{"product":"D"}` with real mapping; P8 same for FUTURE. Pre-fix override tests (MIS/NRML/CNC explicit) unchanged and still pass. **Blockers cleared:** live futures carry and overnight option selling are now mechanically possible; "Live derivatives trading blocked on F&O product model" removed from `PROJECT_STATE.md` Blocked.
*Ref: core/brokers/mapping/upstox.py; tests/brokers/test_upstox_mapping.py (test_to_broker_default_product_by_asset_class); tests/brokers/test_place_order_resolution.py (P7/P8); docs/PROJECT_STATE.md Planned #4 COMPLETE.*

## 2026-06-15 — Phase 4C COMPLETE — 4C.8 RETIRED; FILL_KEY_ALIGNMENT_OPTION_SELECTOR created

Phase 4C (Canonical Instrument Model) is **COMPLETE**. Slices 4C.1–4C.7 all implemented and committed. **4C.8 RETIRED without implementation:** the behavioral goal (zero false reconciliation alerts for an economically-matched position) was achieved upstream via #6b.3 (`core/brokers/token_rekey.py`) — the composition-root token-primary re-key aligns both sides of `reconcile()` to the `NSE_FO|<token>` key space. The original R2 path (modifying `reconciliation.py` to match on `canonical_id`) is retired; `reconciliation.py` remains broker-agnostic by design, superseded by the MM7G/MM7I R1 route decision (locked 2026-06-12). **Replacement item `FILL_KEY_ALIGNMENT_OPTION_SELECTOR`** added to `docs/PROJECT_STATE.md` Planned #8: fix `FillEvent.symbol` to use `ci.instrument_key` on the `execution_mode=option` fill-build path; scope = `handler.py` only; gated on first LIVE `execution_mode=option` strategy. **Architecture record:** R2 retired, R1 canonical, `reconciliation.py` unchanged. **KB sync:** `docs/reports/PHASE_4C_IMPLEMENTATION_PLAN.md` §2 4C.8 marked RETIRED; `docs/PROJECT_STATE.md` Phase 4C COMPLETE + Planned #8 added.
*Ref: docs/reports/PHASE_4C_IMPLEMENTATION_PLAN.md §2; docs/PROJECT_STATE.md Planned #8; core/brokers/token_rekey.py (#6b.3 R1); core/execution/reconciliation.py (unchanged).*

## 2026-06-15 — Phase 4C.7 — broker-payload routing via CanonicalInstrument + fail-fast for unresolved derivatives (532→538 passing; 4C.7 COMPLETE)
First live broker-payload change: `UpstoxAdapter.place_order()` now routes orders through `CanonicalInstrument → UpstoxMapping.to_broker() → BrokerRef` instead of the pre-4C.7 display-symbol fallback (`"instrument_token": order.symbol`, hardcoded `"product": "I"`). **`NormalizedOrder` gains `canonical_instrument: Optional[CanonicalInstrument]` field (4C.7 intentional boundary crossing):** dataclass field + `__init__` param + `object.__setattr__` assignment; NOT serialized (persistence DDL unaffected). **`ExecutionHandler.process_signal` resolves CI at the order-build seam:** Option ENTRY path calls `InstrumentResolver().resolve_option(...)` after `selector.select()`; the else branch resolves CI for FUTURE or OPTION symbols via `resolve_future`/`resolve_option`; the resolved `canonical_instrument` is threaded onto `NormalizedOrder(...)`. Handler does NOT import `core.brokers.mapping` (guard preserved). **`UpstoxAdapter.place_order` uses CI when present, fails fast for derivatives when absent:** `ci = getattr(order, "canonical_instrument", None)` → `self._mapping.to_broker(ci)` → `instrument_token = ref.instrument_key`, `product_code = ref.product_code or "I"`; `LookupError` from `to_broker` → `BrokerContractError`; CI absent + `instrument_type ∈ {OPTION, FUTURE}` → `BrokerContractError` (fail-fast; no silent fallback to display symbol for derivatives); CI absent + EQUITY → `instrument_token = order.symbol`, `product = "I"` (legacy equity path preserved). **`UpstoxMapping` lazy-initialized in `UpstoxAdapter`:** `self._mapping` is `None` at `__init__`, built on first `place_order()` call — prevents `_build_projection()`'s N+1 DuckDB-connection cost from hitting tests that construct `UpstoxAdapter` without calling `place_order()` (eager init in `__init__` caused the full test suite to balloon to 24+ minutes; fixed same session). **G1 guard updated:** `test_canonical_not_imported_on_order_persistence_broker_boundary` removes `order_models.py` from the `boundary` list (intentional crossing documented); `upstox_adapter.py` stays (imports `UpstoxMapping`, not `CanonicalInstrument` — AST check still passes). `test_instrument_key_absent_from_execution` unchanged (new field is `canonical_instrument`, not the forbidden `instrument_key`). `test_order_path_does_not_import_broker_mapping` unchanged (handler doesn't import `core.brokers.mapping`). **New `tests/brokers/test_place_order_resolution.py` — 6 acceptance tests (P1–P6):** F&O option with CI → `instrument_token == "NSE_FO|54710"` not display symbol (P1); product from `BrokerRef.product_code` not hardcoded (P2); equity CI absent → display symbol + `"I"` (P3); option CI absent → `BrokerContractError` no HTTP call (P4); future CI absent → `BrokerContractError` (P5); `LookupError` from `to_broker` → `BrokerContractError` (P6). **5 files changed** (`core/execution/order_models.py`, `core/execution/handler.py`, `core/brokers/upstox_adapter.py`, `tests/g1/test_g1_closure_guard.py`, new `tests/brokers/test_place_order_resolution.py`). **532→538 passing, 0 failing** (+6). 4C.7 COMPLETE.
*Ref: core/execution/order_models.py; core/execution/handler.py; core/brokers/upstox_adapter.py; tests/g1/test_g1_closure_guard.py; tests/brokers/test_place_order_resolution.py.*

## 2026-06-15 — F3 Tick Size Fix — paise→₹ normalization in parse_instruments (531→532 passing; F3 CLOSED)
`parse_instruments()` was storing `tick_size` in paise (the raw Upstox CDN encoding: e.g. `5` for ₹0.05) without the required `/100` conversion. The DB held `5.0` where `0.05` (₹) is correct. **Fix:** one-line change at `scripts/fetch_instrument_master.py:148` — `float(item.get("tick_size") or 0.0)` → `float(item.get("tick_size") or 0.0) / 100`. **No double-scaling:** `resolver.py:230` reads the DB value via `float(tick_size or 0.0)` (no `/100`); once the DB holds ₹ the resolver propagates it correctly. **Lot_size unaffected:** `int(item.get("lot_size") or 0)` is integer-count, no scaling. **Master-readiness unaffected:** the startup gate checks only segment row counts and expiry dates, never tick_size. **Regression test added:** `test_parse_normalizes_tick_size_paise_to_rupees` in `tests/instruments/test_master_ingest.py` — proves `5 paise → ₹0.05`, `100 paise → ₹1.00`, `0 paise → ₹0.00`, `lot_size` unchanged. **Fixture sweep:** 14 test files that pass raw dicts to `parse_instruments()` had `"tick_size": 0.05` (simulating Upstox paise payload) updated to `"tick_size": 5`; existing `assert … == 0.05` assertions unchanged (they check the normalized ₹ output). Scope: `scripts/fetch_instrument_master.py` + 14 test fixture files. **531→532 passing, 0 failing** (+1). F3 CLOSED.
*Ref: scripts/fetch_instrument_master.py:148; tests/instruments/test_master_ingest.py; docs/PROJECT_STATE.md F3.*

## 2026-06-15 — Phase MM7E LIVE Rung — ExecutionMode.LIVE rung: broker injection + broker_positions auto-construction (523→531 passing)
Extended `scripts/fno_runner.build_runner` from the MM7E PAPER rung to the **LIVE rung** — the `ExecutionMode.LIVE` branch that routes orders through a real `BrokerAdapter` and auto-constructs the `broker_positions` callable from the token-rekey chain. **New `broker: Optional[BrokerAdapter]` injection parameter:** `ExecutionMode.LIVE` now requires a real adapter (UpstoxAdapter or equivalent); the old "LIVE without broker_positions" refusal is replaced by the cleaner "LIVE without real broker" refusal (broker_positions flows from the adapter, so the blocker moved upstream). **New `_make_live_broker_positions(adapter)` helper:** returns a callable `() -> List[Dict]` that chains `adapter.get_positions()` → `rekey_broker_positions_by_token()` (MM7J.3, token-primary re-key) → `to_reconcile_positions(rekeyed)` (MM7H shape adapter); positions with `instrument_token is None` are excluded with a WARNING; typed exceptions from `get_positions()` propagate so `LoopDriver._reconcile_ledger` converts them to `RECONCILIATION_FAIL → STOPPED`. **Auto-construction:** if `broker_positions is None` in LIVE mode, the root auto-constructs it from the injected adapter; caller override respected. **Conditional `order_broker`:** `order_broker = broker if LIVE else PaperBroker(clock)` — the handler receives the real adapter for LIVE, PaperBroker for PAPER. **Characterization net (TDD RED→GREEN):** `tests/scripts/_runner_harness.build()` gains `broker=` kwarg (backward-compatible); `test_fno_runner_refusal.py` test renamed + match updated for new semantic; **new `tests/scripts/test_fno_runner_live_rung.py`** — 8 tests (L1–L6): LIVE uses injected broker not PaperBroker; auto-constructs `broker_positions`; callable chains correctly; token-absent excluded + warning emitted; explicit override respected; `BrokerUnavailableError` propagates. Suite: **523→531 passing, 0 failing** (+8). **Scope:** `scripts/fno_runner.py` + 3 test files only — `driver.py`, `handler.py`, `reconciliation.py`, token-rekey, OAuth, WebSocket, options provider unchanged.
*Ref: scripts/fno_runner.py; tests/scripts/test_fno_runner_live_rung.py; tests/scripts/_runner_harness.py; tests/scripts/test_fno_runner_refusal.py; core/brokers/token_rekey.py; core/execution/broker_positions_adapter.py.*

## 2026-06-15 — Phase MM7K.1 — Broker Failure Contract: typed exceptions replace sentinel returns (commit d4dd4d6)
Three typed exception classes (`BrokerAuthError`, `BrokerUnavailableError`, `BrokerContractError`) replace the silent sentinel pattern in `UpstoxAdapter`. `_make_request` is the single classification site: 401/403→`BrokerAuthError` (immediate, no retry); 429 exhausted→`BrokerUnavailableError`; `RequestException` exhausted→`BrokerUnavailableError` wrapping original; loop safety-net→`BrokerUnavailableError`. `get_positions()` drops the bare-except sentinel — raises `BrokerContractError` on non-success response or payload parse error (`KeyError`/`ValueError`/`TypeError`); returns `{}` **only** for genuine flat account (`status=='success'`, `data==[]`); eliminates the flat-vs-failure ambiguity (reconciliation could previously treat any broker outage as a flat account). `get_order_status()` expands status mapping 5→14 documented Upstox values: Terminal (completed/rejected/cancelled), Active (open/trigger-pending/modify-pending/cancel-pending/not-cancelled/not-modified), Submission-in-flight (put-order-req-received/open-pending/validation-pending), AMO (after-market-order-req-received/modify-after-market-order-req-received); unknown status→`BrokerContractError`. `cancel_order()` propagates typed exceptions from `_make_request`. **`tests/brokers/test_upstox_positions.py`:** 2 tests renamed + contract strengthened (`pytest.raises(BrokerContractError)` for non-success; transport stub raises `BrokerUnavailableError`); 1 new test confirms flat-account `{}` return. Suite: **522→523 passing, 0 failing** (+1 net). **Scope:** `upstox_adapter.py` + test file only — driver.py, handler.py, reconciliation.py, token-rekey, OAuth flow, WebSocket ingestor, options provider unchanged.
*Ref: core/brokers/upstox_adapter.py; tests/brokers/test_upstox_positions.py; docs/reports/MM7K0_BROKER_CONTRACT_HARDENING_CHARACTERIZATION.md; commit d4dd4d6.*

## 2026-06-15 — Phase MM7K.0 — Broker Contract Hardening: full characterization of all broker failure paths
Complete audit of all Upstox API interactions, silent failure paths, exception semantics, and LIVE readiness risks — characterization only, no production change. **14-entry silent failure inventory:** `get_positions()` returns `{}` for BOTH "account flat" and any API failure (indistinguishable sentinel); `get_order_status()` returns `SUBMITTED` for all exceptions (bare `except Exception: return OrderStatus.SUBMITTED`); `cancel_order()` returns `False` for both "broker rejected" and network failure; `place_order()` broad `except Exception` re-raises with no typed signal; 9 of 14 documented Upstox order-status strings unmapped (fall to `SUBMITTED` default). **OAuth lifecycle:** token is a short-lived JWT (~24h); adapter holds one token at construction, zero refresh logic; expiry causes HTTP 401 that hits per-method silent swallows. **Risk ranking: 4 CRITICAL** (positions sentinel; order-status sentinel; auth-expiry no auto-refresh; order-placement hides auth vs transport), **4 HIGH** (WebSocket infinite reconnect; cancel-order silent bool; rate-limit exhaustion masked; absent typed exception types), **4 MEDIUM, 2 LOW.** Recommended next: MM7K.1 — classify at `_make_request` source, propagate typed from each public method. **Scope: characterization only — no code, no test, no commit. 522 passing, 0 failing (unchanged from MM7J.3).**
*Ref: docs/reports/MM7K0_BROKER_CONTRACT_HARDENING_CHARACTERIZATION.md; core/brokers/upstox_adapter.py (pre-MM7K.1 state).*

## 2026-06-15 — Phase MM7J.3 #6b.3 — Token-primary reconciliation wiring; UNRECONCILABLE_UNMAPPED_POSITION; acceptance test flipped
Implemented the composition-root re-key that closes the broker-position namespace mismatch for the live path. **New `core/brokers/token_rekey.py` — `rekey_broker_positions_by_token(Dict[str, BrokerPosition]) -> (Dict, List[ReconciliationAlert])`:** re-keys the broker book on `instrument_token` before `to_reconcile_positions()` runs; positions whose `instrument_token is None` are excluded and returned as **`UNRECONCILABLE_UNMAPPED_POSITION`** alerts (distinct from `ORPHANED_BROKER_POSITION` — they cannot be mapped into the reconcile key space at all; cause metadata `internal_value="missing_token"` distinguishes the root cause; `"unknown_token"` reserved for future non-resolvable tokens). Both sides of `reconcile()` now share the same `NSE_FO|<token>` key space — the false double-divergence for an economically-identical position is eliminated. **Located in `core/brokers/` (not `core/execution/`)** so that broker-DTO inspection (`BrokerPosition.instrument_token`) stays in the broker layer — honoring the G1/4C.7 boundary intent; imports `ReconciliationAlert` from `core/execution/reconciliation` (the established `core/brokers → core/execution` direction). **Acceptance test flipped:** `test_mismatched_namespace_same_position_double_false_divergence` (the MM7G smoking-gun pinning 2 false alerts for one matched position) now asserts 0 alerts — the broker payload includes `instrument_token="NSE_FO|53001"`, the re-key makes both sides match, reconcile() is clean. **Two new tests:** `test_none_token_broker_position_is_excluded_and_emits_unreconcilable` + `test_none_token_position_excluded_from_reconcile_engine`. **G1 guards green:** `test_instrument_key_absent_from_execution` passes; no broker-specific DTO references remain in `core/execution/`. **Scope:** new file + test updates only — `reconciliation.py`, `UpstoxAdapter`, `BrokerPosition`, `broker_positions_adapter.py`, G1 guards, `PortfolioView`, runtime behavior unchanged. Full suite **520 → 522 passing, 0 failing** (+2). **trading_symbol confirmed unreliable as reconciliation identity** (compact live form ≠ spaced master form; `instrument_token` is the sole reliable reconcile key). Live-wiring behind ExecutionMode.LIVE remains deferred (gated on first-hand authenticated non-empty position capture).
*Ref: core/brokers/token_rekey.py; tests/runtime/test_reconcile_symbol_namespace.py; docs/PROJECT_STATE.md Planned #6.*

## 2026-06-12 — Phase 6B.ENDPOINT — Broker endpoint correction (net-positions → short-term-positions); #6b.3 still held
The adapter's position read called `GET /v2/portfolio/net-positions`, which against a fresh live OAuth token returns **`HTTP 400 UDAPI100012 "Invalid Endpoint"`** — a dead path. The correct, **live-confirmed** Upstox V2 endpoint is **`GET /v2/portfolio/short-term-positions`** (`200 SUCCESS`). Because `UpstoxAdapter.get_positions()` wraps the call in a bare `except: return {}` (`upstox_adapter.py:153`), the 400 was **silently swallowed on every live call** — live broker positions were empty regardless of actual holdings/positions. **Fix:** one line, `upstox_adapter.py:129` `net-positions → short-term-positions`; parsing, dict keying, and return shape unchanged; the bare-except swallow deliberately retained for the #6b.3 fail-loud design (P3, a distinct `UNRECONCILABLE_UNMAPPED` alert). **520 → 520 passing, 0 failing** (no test asserts the path — all stub `_make_request`; no production module imports the adapter yet). **Supersedes the MM7J.1 endpoint conclusion** — MM7J.1's live probe hit `401` on an expired token and never reached the endpoint, so its `net-positions` schema verdict rests on documentation for a path the live API rejects; `instrument_token` presence / namespace / == ledger key are now **UNVERIFIED** (documentation suggests the field; live verification pending), **not frozen**. **#6b.3 remains HELD.** **Operational prerequisite (not an engineering task):** the authoritative non-empty `short-term-positions` capture is blocked — the live account has **zero balance / zero holdings / zero positions**, so all position endpoints return `data: []` (no schema); capture awaits a real position or sandbox exploration (sandbox = exploratory understanding only, **not** architectural-freeze evidence). Filed `docs/reports/UPSTOX_CANONICAL_API_MAP.md` as the broker-integration source of truth.
*Ref: docs/reports/UPSTOX_CANONICAL_API_MAP.md; core/brokers/upstox_adapter.py:129; tests/brokers/test_upstox_positions.py.*

## 2026-06-12 — Phase MM7J.2 #6b.2 — instrument_token preservation (broker-layer BrokerPosition); broker step of R1
Preserved the broker `instrument_token` through `UpstoxAdapter.get_positions()` — the HARD R1 prerequisite (MM7J.0 P2). It was **DROPPED**: `get_positions()` read only `trading_symbol`/`quantity`/`average_price` and built a plain execution `Position` keyed on `trading_symbol` (`upstox_adapter.py:131-134`), discarding any payload `instrument_token`. Fix: **new `core/brokers/broker_position.py` — `BrokerPosition(Position)`**, a frozen subclass that IS-A `Position` (drop-in for the `BrokerAdapter.get_positions() -> Dict[str, Position]` contract `base.py:27` and the reconcile shape adapter) carrying an added `instrument_token: Optional[str]`, set via `object.__setattr__` to respect the frozen base. `get_positions()` now builds `BrokerPosition(..., instrument_token=pos_data.get('instrument_token'))`, **keeping the dict keyed on `trading_symbol`** (no re-key — that is #6b.3) and defaulting the token to `None` on the legacy equity path. G1-clean: the `instrument_token` literal lives only in `core/brokers/`, never `core/execution/`, so `test_instrument_key_absent_from_execution` stays green; `isinstance(bp, Position)` keeps every consumer + `to_reconcile_positions` unchanged. TDD: +4 characterization tests (token survives `NSE_FO|79381`; existing side/qty/avg/symbol unchanged incl. short→abs; absent-token → `None`, legacy path byte-identical; token-bearing position still shapes through `to_reconcile_positions`) RED (`AttributeError: 'Position' object has no attribute 'instrument_token'`) → GREEN. Full suite **516 → 520 passing, 0 failing** (+4). **Scope: broker layer + adapter tests only — no ExecutionHandler / runtime loop / SignalSource / instrument master / PortfolioView / order-execution-path / reconciliation / shape-adapter change, no re-key, no live wiring.** Next: #6b.3 (composition-root re-key on `instrument_token` + distinct `UNRECONCILABLE_UNMAPPED` alert), gated on a first-hand authenticated payload capture (MM7J.1 §D).
*Ref: docs/reports/MM7J2_INSTRUMENT_TOKEN_PRESERVATION.md; core/brokers/broker_position.py; core/brokers/upstox_adapter.py; tests/brokers/test_upstox_positions.py.*

## 2026-06-12 — Phase MM7J.1 — Upstox net-positions payload verification (instrument_token present = NSE_FO|<token>); R1 holds
Resolved MM7J.0's single open fact before #6b.2 — does the live `/portfolio/net-positions` payload carry `instrument_token` — verification only, no production change. **First-hand live capture was BLOCKED**: the on-disk access token is ~31 days expired (saved 2026-05-12), so a read-only probe of `GET /portfolio/net-positions` returned `HTTP 401 UDAPI100050 "Invalid token"`; no trade placed, no token refreshed autonomously. The verdict therefore rests on the **authoritative Upstox V2 net-positions schema** + adapter design: **`instrument_token` IS present**, format **`NSE_FO|<numeric>`** (e.g. `NSE_FO|52618`) — byte-identical to the master/ledger `instrument_key` (first-hand: master rows are `NSE_FO|98913`-shaped; segment prefixes NSE_FO/MCX_FO/NSE_EQ/NSE_INDEX). **Exact match, no transform.** The live `trading_symbol` is the **compact** form `BANKNIFTY23OCT38000PE` — confirmed absent from the spaced master (`HINDALCO 1230 CE 30 JUN 26`; 0 compact rows), so the fallback is 0% coverage, fallback-only. First-hand resolution probe of the real `UpstoxMapping`: `from_broker_position(instrument_token='NSE_FO|74892')` resolves deterministically to a canonical instrument; compact `trading_symbol` → `None`; spaced form → resolves (but the live payload sends compact). **R1 remains APPROVED** — no R2-forcing condition fired (even a hypothetically missing token forces a broker-layer key fix, not R2, since R2 consumes the same `from_broker_position`). **Remaining gate: one first-hand authenticated capture** (needs an OAuth token refresh + an account holding ≥1 open position, or the Upstox sandbox) — a pre-production-LIVE gate, not a pre-coding blocker; the defensive design surfaces any doc-vs-live mismatch loudly. **Scope: verification only — no code, no test, no commit. 516 passing, 0 failing (unchanged).** Next: #6b.2 broker-layer `instrument_token` preservation (MM7J.2).
*Ref: docs/reports/MM7J1_UPSTOX_PAYLOAD_VERIFICATION.md; core/brokers/upstox_adapter.py; core/brokers/mapping/upstox.py; config/credentials.py; data/instruments/nse_fo_instruments.duckdb (snapshot 2026-06-09).*

## 2026-06-12 — Phase MM7J.0 — R1 preconditions verified against the live master (P1/P2/P3); P2 upgraded to a HARD gate
Verified MM7I's two R1 preconditions empirically against `data/instruments/nse_fo_instruments.duckdb` (snapshot 2026-06-09) before any #6b.2 implementation — verification only, no production change. **P1 (master coverage) splits into two key paths with opposite verdicts.** Via **`instrument_token`**: coverage is **deterministic 100% and format-independent** — `place_order` ships `"instrument_token": order.symbol` (`upstox_adapter.py:86`) and the fill keys the ledger on that same value, so the broker `instrument_token` (`NSE_FO|…`) *equals* the internal ledger key by construction; a token-keyed book matches `reconcile()` directly, no `UpstoxMapping` lookup needed. Via the **`trading_symbol` fallback**: **unverifiable offline and likely 0%** — the master `tradingsymbol` is the **spaced** form `NIFTY 20350 CE 30 JUN 26` (sourced verbatim from Upstox's instrument JSON, `fetch_instrument_master.py:140`), and the **compact** form MM7G/MM7H/MM7I assumed (`NIFTY26JAN2623500CE`) returns **0 rows**; whether live net-positions `trading_symbol` matches the spaced master form is not observable from any repo fixture (`test_upstox_positions.py` uses only equity `RELIANCE`/`TCS`, no derivative, no `instrument_token`). Master-side projection is **complete** (41,227/41,227 NSE_FO FUT/CE/PE rows non-null + resolvable) and **1:1** (0 collisions across 59,241 distinct resolvable `tradingsymbol`s) — **clearing two of MM7I §7's R2-forcing conditions** (non-1:1 map; incomplete projection). **P2 (instrument_token):** exists per the V2 schema and `from_broker_position`'s preferred key (`upstox.py:75`) but **unconfirmed by any fixture**, and **discarded today** (`upstox_adapter.py:131-134` reads only `trading_symbol`/`quantity`/`average_price`); smallest legal preservation = re-key the returned dict on `instrument_token` inside the `get_positions()` loop (`upstox_adapter.py:131-149`, broker layer — imports no `CanonicalInstrument`, trips neither G1 guard); an `instrument_key`-named field on `Position` is illegal (`test_g1_closure_guard.py:333-340`). **P2 is upgraded from MM7I's "noted follow-up" to a HARD R1 prerequisite** — R1's soundness rests entirely on the token path. **P3 (unmapped policy challenge):** fail-loud → orphan is correct **only atop** the deterministic token path (where "unmapped" is near-impossible for a position the platform itself opened); on a `trading_symbol` format miss it is catastrophic (100% of positions orphan → every-run startup refusal masquerading as a fleet of orphans). Recommend a **distinct `UNRECONCILABLE_UNMAPPED_POSITION` alert** (keep fail-loud, fix diagnostics) over a plain `ORPHANED_BROKER_POSITION`. **R1 remains APPROVED** behind a revised four-item gate: confirm payload `instrument_token`; preserve + key on it; `trading_symbol` fallback-only; distinct unmapped alert + fail-loud. **Single highest-value next action: capture one authenticated `/portfolio/net-positions` response** to confirm (a) `instrument_token` presence and (b) the live `trading_symbol` format — the one open fact that, if it shows no `instrument_token`, forces a broker-layer key fix before either route. **Scope: verification only — no code, no test, no commit, no adapter/mapping/reconciliation change. 516 passing, 0 failing (unchanged).** Next: #6b.2 — implement R1 behind the §4 gate, beginning with the `instrument_token` broker-layer preservation + the captured-payload confirmation.
*Ref: docs/reports/MM7J0_R1_PRECONDITIONS.md; docs/reports/MM7I_NAMESPACE_ROUTE_DECISION.md (§4.4, §7); core/brokers/upstox_adapter.py:86,131-149; core/brokers/mapping/upstox.py:75; scripts/fetch_instrument_master.py:140; data/instruments/nse_fo_instruments.duckdb (snapshot 2026-06-09).*

## 2026-06-12 — Phase MM7I — Namespace-mapping route FROZEN = R1 (adapter-local instrument_key remap); decision only
Froze the #6b.2 symbol-namespace route before implementation — decision + report only, no production change. **Route = R1** (re-key the broker book `trading_symbol → instrument_key` at the composition root, reusing the already-built `UpstoxMapping`; `reconcile()` untouched) over **R2** (full 4C.8 canonical↔canonical rewrite, blocked on resuming the paused 4C.6→4C.8 chain). **I1 boundary:** R1 crosses neither G1 guard *iff placed correctly* — the translation primitive in `core/brokers/mapping/` (where `instrument_key` is legal, `upstox.py:57,60,75`) and the binding lambda at the `scripts/fno_runner.py` composition root (outside `core/`); putting it in `core/execution/` would fail both `test_instrument_key_absent_from_execution` and `test_order_path_does_not_import_broker_mapping` (`test_g1_closure_guard.py:333-352`). **I2 smallest location:** mapping layer (one public `trading_symbol→instrument_key` accessor; `UpstoxMapping` exposes only the private `_ikey_by_tradingsymbol` + `from_broker_position`→Canonical today) + runtime/composition-root binding; rejected `adapter` (G1) and `broker` (`CanonicalInstrument`-import ban + discards `instrument_token`). **I3 revert:** fully reversible — zero persisted state (reconcile is in-memory, startup-gate-only, `driver.py:462-494`); the shape adapter + `instrument_token` preservation are retained by 4C.8 anyway, the accessor + closure delete in two edits. **I4:** R1 makes 4C.8 strictly easier/neutral — it writes nothing into `reconciliation.py`, the ledger keying, or persistence (the three surfaces 4C.8 changes), only removable code in the layers 4C.8 leaves behind; the restored-ledger key is preserved today (`handler.py:284-287` `replace_instrument` keeps the key), so R1's "both sides → instrument_key" target holds. **I5 acceptance:** flip `test_mismatched_namespace_same_position_double_false_divergence` 2 alerts → 0; re-express the aligned/orphan cases for the new instrument_key broker namespace; add accessor + unmapped-policy tests; keep the G1 guards + shape-adapter nets green. **Challenge:** R1 is not unconditionally safe — coverage gaps, `trading_symbol` format drift, a ledger re-key to `canonical_id`, or a non-1:1 symbol map would flip the call to R2 (these are the conditions MM7J.0 then tested). Recommended unmapped policy: fail-loud. **Scope: decision only — no code, no test, no commit, no adapter/reconciliation/broker/4C.8 change. 516 passing, 0 failing (unchanged).** Next: MM7J.0 (verify the R1 preconditions) → #6b.2 (implement R1).
*Ref: docs/reports/MM7I_NAMESPACE_ROUTE_DECISION.md; docs/reports/MM7G_NAMESPACE_CHARACTERIZATION.md; docs/reports/MM7H_SHAPE_ADAPTER_REPORT.md; core/brokers/mapping/upstox.py; core/execution/reconciliation.py; scripts/fno_runner.py; tests/g1/test_g1_closure_guard.py.*

## 2026-06-12 — Phase MM7H #6b.1 — Broker-position SHAPE adapter (Dict[str,Position] → List[Dict])
Implemented the shape half of #6b — **new `core/execution/broker_positions_adapter.py`** with one pure function `to_reconcile_positions(Dict[str, Position]) -> List[Dict]`. Resolves the two-ended contract mismatch (MM7F F1): container `Dict[str, Position]`→`List[Dict]`, `side` `PositionSide` enum→string via `pos.side.value`, `quantity` carried through (already absolute — `reconcile()` re-signs from `side`, reconciliation.py:48-51), **symbol = the dict KEY verbatim**. LONG/SHORT/FLAT preserved exactly. It is the single production home for the transform that previously existed only as duplicated test glue (`_bridge`/`_broker_positions_as_dicts`; MM7F F3) — the MM7A (`test_broker_positions_adapter.py`), Phase-0 (`test_reconciliation_broker.py`), and MM7G (`test_reconcile_symbol_namespace.py`) nets' bridges are repointed to delegate to it (no assertion changed; they now exercise the real function end-to-end through the `UpstoxAdapter` shape). **Namespace mapping deliberately excluded (#6b.2):** the broker `trading_symbol` key passes through unchanged, so wired live as-is it would still orphan every position (MM7G §2.1) — expected and safe because **6b.1 ships no live wiring** (`fno_runner` still refuses `ExecutionMode.LIVE` without `broker_positions`; paper rung runs `broker_positions=None`). Kept shape-only on three grounds: single-responsibility (shape is total + broker-agnostic; namespace mapping is partial + broker-specific), the R1-vs-R2 route is an unmade 6b.2 decision, and the **G1/4C.7 boundary forbids `instrument_key` in `core/execution/`** — the closure guard `test_instrument_key_absent_from_execution` enforces it and caught a docstring use of the literal token during TDD (removed). TDD: new `tests/execution/test_to_reconcile_positions.py` (7 unit tests) RED (`ModuleNotFoundError`) → GREEN. Full suite **509 → 516 passing, 0 failing** (+7). **Scope: shape only — no namespace mapping, no live wiring, no reconciliation/broker change, no 4C.8/F3/F4.** Next: #6b.2 (namespace mapping, route R1 via the already-built `UpstoxMapping` / vs R2 full 4C.8; acceptance = flip the mismatch test 2 alerts → 0), then #6b.3 (live wiring behind F4).
*Ref: docs/reports/MM7H_SHAPE_ADAPTER_REPORT.md; core/execution/broker_positions_adapter.py; tests/execution/test_to_reconcile_positions.py; tests/execution/test_broker_positions_adapter.py; tests/execution/test_reconciliation_broker.py; tests/runtime/test_reconcile_symbol_namespace.py; commit a366d77.*

## 2026-06-12 — Phase MM7G — Broker-position namespace characterization (G1–G5); #6b foundation status corrected
Characterized the broker-position vs execution-ledger symbol-namespace problem before any #6b adapter work — characterization only, no production change. **Three namespaces are in play and `reconcile()` matches them by raw string** (`reconciliation.py:54,57,77`): (1) internal ledger key = `instrument_key`/order `instrument_token` (e.g. `NSE_FO|53001`, `PositionTracker._positions` keyed via `fill.symbol`, `handler.py:326`; the token UpstoxAdapter.place_order ships, `upstox_adapter.py:86`); (2) Upstox position symbol = broker `trading_symbol` (e.g. `NIFTY26JAN2623500CE`, `upstox_adapter.py:131-132` — the raw payload's `instrument_token` is **discarded**); (3) canonical `canonical_id`. **G1** — PaperBroker key ≡ ledger key (both `PositionTracker`/`fill.symbol`) but the PaperBroker book is permanently empty (`place_order` never updates `self.tracker`, `paper_broker.py:22,49`): no transformation, no divergence. **G2** — a live broker position cannot be reconciled today without transformation, twice over: shape/type (enum→string, raises) **and** namespace (`trading_symbol`≠`instrument_key`). **G3** — `ORPHANED_BROKER_POSITION` first occurs at `reconciliation.py:77-85`, reachable in production only when #6b wires a shape-only live source. **G4 / 4C.8 verdict = C (required before #6b), correcting MM7F assumption C5:** the mapping mechanism `UpstoxMapping.from_broker_position` (4C.4) is **already built + tested** (`core/brokers/mapping/upstox.py:74-81`; `tests/brokers/test_upstox_mapping.py`); only the reconcile-wiring (4C.8) is paused. #6b can reach parity via **R1** (adapter-local `trading_symbol→instrument_key` remap, **unblocked**) without the full 4C.8 canonical↔canonical rewrite (**R2**) — the full 4C.8 slice is not a hard #6b prerequisite. **G5** — new `tests/runtime/test_reconcile_symbol_namespace.py` (4 tests, pin current behavior): namespace match → `[]`; matching-ns qty mismatch → 1 `QUANTITY_MISMATCH`; orphan → 1 `ORPHANED_BROKER_POSITION`; **namespace mismatch → 2 false alerts for ONE identical economic position** (the smoking gun the existing matching-`"RELIANCE"` net masks). Full suite **505 → 509 passing, 0 failing** (+4). **Scope: characterization only — no adapter, no reconciliation change, no broker change, no 4C.8/F3/F4 work.** Next: #6b (route-decision R1/R2 → shape consolidation → key-map → live-wire behind F4).
*Ref: docs/reports/MM7G_NAMESPACE_CHARACTERIZATION.md; tests/runtime/test_reconcile_symbol_namespace.py; core/execution/reconciliation.py; core/brokers/upstox_adapter.py; core/brokers/mapping/upstox.py; commit 773b641.*

## 2026-06-12 — Phase MM7F #6a — W3 gate hardening: a raising broker_positions is a startup refusal (T3 net flipped); W3 CLOSED
Closed hazard W3 (MM7_LIVE_WIRING_REVIEW.md §3.2): a raising `broker_positions()` in `LoopDriver._reconcile_ledger` used to propagate **uncaught** out of `run()`, stranding the driver in RECOVERY with no STOPPED, no journal, no alert (the Phase-F gate runs before `run()`'s `try/finally`, driver.py:524). The single broker-book read is now wrapped — any exception (broker auth/transport — `upstox_adapter.py:60-62` RuntimeError on 401/403) routes into the **existing** refusal contract (mirrors the RECONCILIATION_FAIL divergence path driver.py:465-475 and the master-BLOCK path :394-403): emit `RECONCILIATION_FAIL` → `alerter.critical` → `abort_startup()` → STOPPED; the gate returns False so the loop never runs (`bars_processed == 0`). `except Exception` (not bare) keeps KeyboardInterrupt/SystemExit propagating. **Flipped the MM7A T3 net** (`tests/runtime/test_driver_broker_positions_failure.py`) from defect-pin to refusal-pin: no escape, STOPPED, RECONCILIATION_FAIL + STOPPED journaled, critical alert; `bars_processed == 0` kept. TDD (RED 3-fail at the driver.py:464 escape → GREEN). Full suite **505 passing, 0 failing** (count unchanged — assertions flipped, population unchanged). **Preceded by the MM7F adapter-path review** (`MM7F_BROKER_POSITIONS_ADAPTER_REVIEW.md`): adapter-necessity verdict **PARTIAL** (a dict→list + enum→string transform is mandatory before any *real* broker book reaches reconcile — pure pass-through provably raises — but it is a ~4-line fn and NOT on the MM7E paper rung's critical path: at ExecutionMode.PAPER `broker_positions=None` is correct because PaperBroker's tracker is never updated by `place_order`, so its book is permanently empty and wiring it manufactures false QUANTITY_MISMATCH); owner = a standalone `to_reconcile_positions` fn wired by the composition root; W3 is independent of the adapter (a driver-gate property). **Scope: gate hardening only — no adapter (#6b), no reconciliation-logic change, no broker change, ExecutionMode.LIVE untouched.** Next: #6b (broker-positions shape adapter + the broker `trading_symbol`↔internal-key namespace decision, bundled with the ExecutionMode.LIVE rung behind F4).
*Ref: docs/reports/MM7F_6A_W3_GATE_HARDENING_REPORT.md; docs/reports/MM7F_BROKER_POSITIONS_ADAPTER_REVIEW.md; core/runtime/driver.py; tests/runtime/test_driver_broker_positions_failure.py; commit d3a4390.*

## 2026-06-12 — Phase MM7E — Composition root implemented (scripts/fno_runner.py, Design B); MM7A T1 flipped tripwire → acceptance
Landed the first production composition root, the file that did not exist through all of MM7A–D.1: **`scripts/fno_runner.build_runner(*, source, symbols, underlyings=None, …)`** constructs the four mechanical collaborators (`LoopDriver`, `ExecutionHandler` `load_db_state=True`, `PaperBroker`, `LiveDuckDBMarketDataProvider`) and **accepts the `SignalSource` by dependency injection** — it constructs no source (ADR-MM7E-1 / Design B). Targets **`Mode.LIVE` + `ExecutionMode.PAPER`** (full startup gate / restore-canonicalization / watchdog / reconciliation against the real broker book, no capital moved). **Semantic refusals the driver does not own (MM7E §4):** `source is None` → refuse (the T1 `source present` clause); `has_derivatives(symbols)` without `underlyings` → refuse (the W4 trap — no checker disables both the readiness gate AND G1 restore-canonicalization); `ExecutionMode.LIVE` without `broker_positions` → refuse (#6's territory, named not built). A vacuous `ExecutionMode.PAPER` reconcile **warns** (warn > refuse). **`build_master_readiness(underlyings, db_path=…)` is injected iff `has_derivatives`** (the activation switch for G1's restore half, W4 / Finding E1-c), `None` for an equity universe (the carve-out) — and this is the live call-site MM.7 blocker #4 was missing (the checker is now invoked by a production module, not only a test). The driver retains the structural refusals (clock/provider/LIVE-handler inside `run()`); `clock`/`provider`/`db_manager`/`journal`/`metrics_path`/`max_bars` are optional injection seams defaulting to the production construction (the same DI the driver uses, config.py:9-13). **Characterization net (§6):** new `tests/scripts/_runner_harness.py` (MM7C/MM7D.1 isolation — monkeypatch `handler_mod.ExecutionStore` → tmp, tmp `DatabaseManager`, `ReplayClock`, fixture master, test-local `NoopSource`/`BuyExitSource`) + `test_fno_runner_composition.py` (5: handler load_db_state, PaperBroker, Mode.LIVE+PAPER, real checker FRESH for F&O / omitted for equity, injected source carried) + `test_fno_runner_refusal.py` (4: the §4 contract) + `test_fno_runner_activation_path.py` (2: the E5 sequence `READINESS→CANONICALIZE_POSITIONS→CANONICALIZE_ORDERS→RECONCILE` fires end-to-end via the production root on a FRESH F&O run; the equity paper rung runs the MM7D.1 BUY/EXIT spine to a clean STOPPED — 2 orders, 2 fills, FLAT). **MM7A T1 converted:** the two absence tripwires in `test_fno_entry_wiring.py` flipped to acceptance (the script exists, is the runner entry point, and the production-root-built driver satisfies every `_fno_live_contract` clause); the doubles predicate + 5 rejection cases retained. **MM7D.1 test 8 narrowed** (its duplicated "no scripts/ LoopDriver" assertion removed; now asserts only its own test-locality). The G1 closure guard (`core/`-scoped) is untouched — `fno_runner` uses the normal `process_signal` path, so the `process_group_signal`/`_check_greek_limits` carve-outs stay dead. TDD (RED `ModuleNotFoundError: scripts.fno_runner` → GREEN); full suite **493 → 505 passing, 0 failing**. **No production `SignalSource`/strategy/broker adapter; F3/F4/#4/#5/4C.7 untouched; `ExecutionMode.LIVE` out of scope.**
*Ref: docs/reports/MM7E_IMPLEMENTATION_REPORT.md; docs/reports/MM7E_ENTRY_SCRIPT_REVIEW.md (§10); docs/ARCHITECTURE_DECISIONS.md (ADR-MM7E-1); scripts/fno_runner.py; tests/scripts/_runner_harness.py, test_fno_runner_composition.py, test_fno_runner_refusal.py, test_fno_runner_activation_path.py, test_fno_entry_wiring.py; tests/runtime/test_synthetic_wiring_proof.py.*

## 2026-06-12 — Phase MM7E — Entry-script review + MM7E.1 composition-root scope challenge (Design B LOCKED; review only)
Mapped the first production composition root (the F&O entry script that constructs the live `LoopDriver` — the Planned #4 "4-residual" live-enablement track surfaced by MM.7) and ratified its source-ownership scope. **No code, no `scripts/`, no `SignalSource`, no tests, no commit.** The review (`docs/reports/MM7E_ENTRY_SCRIPT_REVIEW.md`) maps: the five E1 objects the root must construct (`ExecutionHandler` `load_db_state=True` → `PaperBroker` → `RealTimeClock` → live `MarketDataProvider` → `build_master_readiness`); the E3 dependency table (the two genuinely-missing production pieces are `source`/W1 and `broker_positions`/W2-#6 — everything else exists); the **E2 runtime recommendation `Mode.LIVE` + `ExecutionMode.PAPER` first** (exercises the full startup gate / canonicalization / watchdog with no capital moved — equity inner rung proving the root, then F&O rung + `build_master_readiness([...])` for gate teeth, both inside MM7E; `ExecutionMode.LIVE` out of scope, blocked on F4/#4/#5); the E4 refusal contract (semantic refusals at the **composition root** — `source is not None`, `master_readiness` present when `has_derivatives`, `broker_positions` before `ExecutionMode.LIVE`; the driver already refuses clock/provider/LIVE-handler inside `run()`); the E5 G1-protected activation sequence (readiness→canonicalize→reconcile — injecting `build_master_readiness` is the activation switch for G1's restore-canonicalization half, W4/Finding E1-c); and the E6 characterization net (convert the MM7A T1 tripwire to the script's acceptance assertion + three new `tests/scripts/` files). **MM7E.1 decision — Design B (inject, don't construct the source) LOCKED.** The root constructs the **four** mechanical collaborators and **accepts `SignalSource` via dependency injection** with a not-None refusal; it does **not** own source creation. Design B beats Design A (root builds a "minimal deterministic" source) on all five challenge axes — **risk** (zero source-risk surface: no alpha, no MM7C C4 chain-parity, no F4 enters MM7E), **coupling** (depends only on the C1–C6 consumer protocol, no import edge to any strategy module), **MM7D.1 fidelity** (the production-shaped continuation of the exact injection seam MM7D.1 proved — `source` is already a `LoopDriver.__init__` param), **smallest root** (four objects not five; honestly a *partial* root — terminal source construction relocates to the later strategy slice), **W1 removal** (W1 leaves MM7E's critical path entirely, becoming solely the strategy slice's concern). **MM7E does NOT require a production `SignalSource` — it requires the seam for one.** This sharpens the review body's §7-1/§8.1 mitigation from "ship a minimal source" (Design A-lite, residual coupling) to "inject the source"; it does not alter the runtime target (§3), refusal contract (§4), activation sequence (§5), or net (§6). First concrete source (minimal or strategy) deferred to a later slice that wires the terminal root.
*Ref: docs/reports/MM7E_ENTRY_SCRIPT_REVIEW.md (§10 scope challenge; §0–§9 entry-script map); docs/reports/MM7_LIVE_WIRING_REVIEW.md; docs/reports/MM7A_CHARACTERIZATION_REPORT.md; docs/reports/MM7C_SIGNALSOURCE_CHARACTERIZATION.md; docs/reports/MM7D1_SYNTHETIC_WIRING_PROOF.md; docs/PROJECT_STATE.md (Completed — Phase MM7E).*

## 2026-06-11 — GATE G1 CLOSED — Wave 5 closure guard + final closeout audit (identity-architecture program complete)
The mechanical closure proof and the formal gate flip. **Wave 5** delivered `tests/g1/test_g1_closure_guard.py` (19 tests — AST + grep + characterization, `SOLE_IDENTITY_PATH_REVIEW.md` §6 criterion #5): it FAILS if any new `InstrumentParser.parse` / direct legacy `Option(...)`/`Future(...)` construction becomes reachable from the live F&O order-build or post-gate-restore path, or if a `CanonicalInstrument` crosses the `NormalizedOrder` / persistence / broker-payload boundary. It pins — the order-build entry constructs no legacy identity (option ENTRY→`OptionsContractSelector.select`, derivatives→`canonicalize_symbol`, `InstrumentParser.parse` only as the guarded `IfExp.orelse` equity fallback); a repo-wide AST inventory classifies every `parse`/`Option(`/`Future(` site in `core/` against an audited allowlist (6 parse files, 4 construction files, all carve-out/dead/restore-at-construction/whitelisted-derivation); the three derivation points resolve through `InstrumentResolver`; `instrument_key` absent from `core/execution/`; `CanonicalInstrument` not imported on the order/persistence/broker boundary; `handler.py` does not import `core.brokers.mapping`; the restore upgrade runs strictly after `_check_master_readiness` and before `_reconcile_ledger` and is gated like MM.4; #6 `Position(symbol=)` has no production caller. The **final closeout audit** (`G1_CLOSEOUT_REPORT.md`) independently confirmed via call graph that the live order funnel is single and canonical: `process_signal`'s only runtime caller is `LoopDriver._dispatch_signals` (ADR-006), and the parse-using `process_group_signal` (#2 batch) + `_check_greek_limits` (#3) carve-outs have **no live caller** (no F&O entry script constructs a live `LoopDriver` — the only `LoopDriver(...)` reference outside tests is a docstring). Full suite **422 → 441 passing, 0 failing**. **Verdict flipped OPEN → CLOSED (2026-06-11): `CanonicalInstrument` is the sole identity source on the live F&O path — "No."** G1 was behavior-preserving (broker payload byte-for-byte unchanged); 4C.7 stays blocked. The critical path now shifts off identity onto live-enablement: MM.7 live wiring (F&O entry script), F3 (tick-size paise scaling), F4 (lot 65/30 exchange verification), F&O product/segment model + SPAN margin.
*Ref: docs/reports/G1_WAVE5_CLOSURE_GUARD_REPORT.md; docs/reports/G1_CLOSEOUT_REPORT.md; docs/reports/SOLE_IDENTITY_PATH_REVIEW.md (§6, verdict flip); tests/g1/test_g1_closure_guard.py; core/execution/handler.py; core/runtime/driver.py.*

## 2026-06-11 — Gate G1 Wave 4 (#4 forward option O1/O2) + Wave 4B (#7 forward position) — forward identity sites migrated
Migrated the two remaining **forward** identity-construction sites, behavior-preserving. **#4/O1** (`core/execution/options/selector.py`): `OptionsContractSelector.select` now *derives* the legacy `Option` from a resolver-sourced `CanonicalInstrument` (reads `ci.lot_size`) instead of reading the lot and discarding the CI to hand-build the `Option`; the symbol stays selector-computed (`_build_symbol`, never `ci.display_symbol`) so the broker payload byte is preserved, `lot_size_override` precedence and the `INDEX_LOT_SIZES` master-absent fallback (ADR-003 — type never flips on DB presence) are intact. **#4/O2** (`handler.py:581-583` else branch): an option-shaped EXIT symbol routes through `canonical_restore.canonicalize_symbol` (→ master-lot `Option`) before falling back to `InstrumentParser.parse`, closing the resolver-blind lot-1 EXIT identity gap (EXIT sizing is position-sourced, so no payload change). **#7** (`handler._handle_broker_fill`): the open *forward* position's identity is canonicalized at the fill seam via the same `canonicalize_symbol` primitive (futures EQUITY→FUTURE with master multiplier, option lot 1→master lot, symbol/key preserved), so a forward-built position and its restored twin carry byte-identical canonical identity — no restart drift; equity/unresolved → left legacy (carve-out). `CanonicalInstrument` stays internal throughout (the G1 / 4C.7 boundary). Characterization nets (`test_g1_wave4a1_option_characterization.py`, `test_g1_wave4b_position_characterization.py`) green before and after; the intended axes (option lot 1→65, futures Equity→Future / mult ×65, forward-vs-restored parity) were the only assertion deltas, each justified.
*Ref: docs/reports/G1_WAVE4_OPTION_PATH_REVIEW.md; docs/reports/SOLE_IDENTITY_PATH_REVIEW.md (§2 DERIVE, sites #4/#7); core/execution/options/selector.py; core/execution/handler.py; core/execution/canonical_restore.py; tests/execution/test_g1_wave4a1_option_characterization.py; tests/execution/test_g1_wave4b_position_characterization.py.*

## 2026-06-10 — Gate G1 Wave 3 (Restore Path #2) — restored ORDER canonicalization (#8) implemented — Restore Path COMPLETE
Implemented the order half of the Option-B post-gate pass, the sibling of the prior `#7-as-restored` position commit and the **last remaining legacy restore identity path** (Wave 3B §3 decided orders/positions migrate as two independent commits, in either order). **New `OrderTracker.replace_instrument(order_id, instrument)`**: swaps a tracked order's `.instrument` **in place** via `object.__setattr__` on the frozen `NormalizedOrder` — preserving `correlation_id`/`signal_id`/`side`/`quantity`/`order_type` and the tracker key (H7/H8: the order is mutated, never reconstructed, so idempotency `_seen_signals` and group membership keyed on `correlation_id` stay intact); the new instrument carries the same `.symbol`, so the order's symbol is unchanged (H3). **New `OrderTracker.order_states()`**: the order-side analogue of `PositionTracker.get_all_positions`, so the handler can iterate restored orders. **New `ExecutionHandler.canonicalize_restored_orders(resolver=None)`**: re-resolves each tracked derivative order's symbol through the gate-verified master via the existing `canonical_restore.canonicalize_symbol` primitive (shared with #7) and calls `replace_instrument` — futures EQUITY→FUTURE (H1), option parser-lot 1→master lot (H2); equity/unresolved symbols left legacy (carve-out). **`LoopDriver._canonicalize_restored_ledger` extended**: the same live-F&O gate-pass (`is_live ∧ has_derivatives ∧ master_readiness is not None`, AFTER readiness, BEFORE reconciliation) now triggers `canonicalize_restored_orders()` alongside the existing `canonicalize_restored_positions()`; paper/replay/equity-only-LIVE and no-checker/BLOCK stay a no-op. **Option B preserved end-to-end:** `_replay_state` is unchanged and still builds legacy identity at construction, so the Wave 3A characterization suite (pre-gate reality: futures order→EQUITY, option order lot=1) stays green untouched — canonical identity appears ONLY after the gate. `CanonicalInstrument` stays internal (the G1 / 4C.7 boundary). TDD (RED→GREEN): +4 handler-level tests (`tests/execution/test_g1_restore_order_canonicalization.py` — future order→FUTURE w/ master multiplier, option order lot 1→75 from an injected master with a lot distinct from both the parser default 1 and production F4 65, correlation_id/signal_id/tracker-key preserved across the swap, equity carve-out) + 3 driver-wiring tests (`test_driver_gate_ordering.py` — gate invokes order canonicalization on FRESH, skips it without a checker and on BLOCK), all RED before the methods existed. Full suite **402 → 409 passing, 0 failing**. **Both restore halves (#7-as-restored positions + #8 orders) are now canonicalized post-gate — Migration Target #2 (Restore Path) is COMPLETE.** **Gate G1 remains OPEN** pending the remaining order-construction sites (#6/#7 forward) and the Wave 5 AST/grep closure proof; a formal G1 closeout audit is the recommended next step before Wave 4 (#4 option path).
*Ref: docs/reports/G1_WAVE3_RESTORE_REVIEW.md (§3, §6 step 4); docs/reports/G1_WAVE3B_GATE_ORDERING_REVIEW.md (§3, §6); docs/reports/SOLE_IDENTITY_PATH_REVIEW.md (§3 Option B, sites #8); core/execution/order_tracker.py; core/execution/handler.py; core/runtime/driver.py; tests/execution/test_g1_restore_order_canonicalization.py.*

## 2026-06-10 — Gate G1 Wave 3 (Restore Path #2) — restored POSITION canonicalization (#7-as-restored) implemented
Implemented the position half of the Option-B post-gate pass — the corruption-relevant half (greeks/margin consume `position.instrument`), chosen first per Wave 3B §3.6. **New `core/execution/canonical_restore.canonicalize_symbol(symbol, timestamp, resolver)`**: the restore-path resolution primitive — futures via the existing `resolve_future` (Wave 2 #1; always derives a `Future`, master-absent too, ADR-003), options via the NSE option-symbol shape → `InstrumentResolver.resolve_option` → derive a legacy `Option` with the **master lot**; returns None for equity/unresolved symbols (carve-out, leave legacy). `CanonicalInstrument` stays internal — only expiry/lot/underlying are read (G1/4C.7 boundary). **New `PositionTracker.replace_instrument(symbol, instrument)`**: swaps a tracked position's `.instrument` while preserving side/quantity/avg_price/last_updated and the symbol key — the in-place identity swap, owned by the tracker (position truth, ADR-001); does not touch `get_position`/#7 source (H5) or the snapshot table/#9 (H6). **New `ExecutionHandler.canonicalize_restored_positions(resolver=None)`**: re-resolves each tracked derivative position's symbol and calls `replace_instrument` — futures EQUITY→FUTURE (H1), option lot 1→master lot (H2), symbol byte-for-byte preserved so reconciliation still matches (H3). **`LoopDriver._canonicalize_restored_ledger` wired** (was the no-op slot from the prior commit): on the live-F&O gate-pass — gated on the SAME condition as MM.4 (`is_live ∧ has_derivatives ∧ master_readiness is not None`) — it triggers `handler.canonicalize_restored_positions()` AFTER readiness, BEFORE reconciliation; paper/replay/equity-only-LIVE and the no-checker/BLOCK paths stay a no-op (the ledger mutation is the handler's; the driver only triggers it in the slot). **Option B preserved end-to-end:** restore-at-construction (`_replay_state`) is unchanged and still builds legacy identity, so the Wave 3A characterization suite (pre-gate reality: futures→EQUITY, option lot=1) stays green untouched — canonical identity appears ONLY after the gate. **Restored ORDER identity (#8) is NOT in this wave** — the separate, independently-revertible commit (Wave 3B §3). TDD (RED→GREEN): +4 handler-level tests (`tests/execution/test_g1_restore_canonicalization.py` — future→FUTURE w/ master multiplier, option lot 1→75 from an injected master with a lot distinct from both the parser default 1 and production F4 65, symbol-preserved reconciliation PASS, equity carve-out) + 3 driver-wiring tests (`test_driver_gate_ordering.py` — gate invokes canonicalization on FRESH, skips it without a checker and on BLOCK), all RED with `AttributeError` before the methods existed. Full suite **395 → 402 passing, 0 failing**. **Gate G1 remains OPEN; the #8 (orders) in-place-swap wave — the remaining SEPARATE commit — NOT STARTED.**
*Ref: docs/reports/G1_WAVE3B_GATE_ORDERING_REVIEW.md (§3, §6 step 5); docs/reports/G1_WAVE3_RESTORE_REVIEW.md (§3, §6); docs/reports/SOLE_IDENTITY_PATH_REVIEW.md (§3 Option B, sites #7/#9); core/execution/canonical_restore.py; core/execution/handler.py; core/execution/position_tracker.py; core/runtime/driver.py; tests/execution/test_g1_restore_canonicalization.py.*

## 2026-06-10 — Gate G1 Wave 3 (Restore Path #2) — Option-B canonicalization slot introduced (documented no-op)
First production-code step of Migration Target **#2 (Restore Path)**: introduced `LoopDriver._canonicalize_restored_ledger()` as a **documented no-op at the mapped slot** (`G1_WAVE3B_GATE_ORDERING_REVIEW.md` §1/§6 step 3). The call is inserted in `_run_startup_gate` **strictly after `_check_master_readiness()` proves the master present and strictly before `_reconcile_ledger()`** (`core/runtime/driver.py`), so the Option-B post-gate canonicalization pass has a real, tested insertion point before any identity-swapping logic exists. **Method body is inert** (docstring + `return`): when implemented it will re-resolve each live-F&O restored ORDER (#8, `order_repository.py:60`) and tracked POSITION (#7-as-restored, `position_tracker.py:31`) identity through `InstrumentResolver` and swap `.instrument` **in place** (preserve symbol/correlation_id/signal_id/side/qty/order_type — H7/H8; symbol byte-for-byte for reconciliation keying — H3), gated on the same condition as MM.4 (LIVE ∧ derivatives ∧ master-ready), no-op for paper/replay/equity; it must not touch `get_position`/#7 (H5) or wire `load_all`/#9 (H6). Runs on a gate-pass only (FRESH/WARN); a BLOCK aborts before it. **No behavior change** — the no-op fires in the slot but does nothing, so every existing gate/startup/reconciliation test is unchanged. TDD (RED→GREEN): +2 tests in `tests/runtime/test_driver_gate_ordering.py` (`test_canonicalize_hook_runs_in_the_slot_on_fresh` — production method spied, `seq == ["READINESS","CANONICALIZE","RECONCILE"]`; `test_canonicalize_hook_not_reached_on_block` — `seq == ["READINESS"]`, hook unreached on a refused start), both RED with `AttributeError` before the method existed. Full suite **393 → 395 passing, 0 failing**. **Gate G1 remains OPEN; the #8 (orders) and #7-as-restored (positions) in-place-swap waves — the SEPARATE, independently-revertible commits decided in Wave 3B §3 — are NOT STARTED.**
*Ref: docs/reports/G1_WAVE3B_GATE_ORDERING_REVIEW.md (§1, §3, §6); docs/reports/G1_WAVE3_RESTORE_REVIEW.md (§6 step 3); docs/reports/SOLE_IDENTITY_PATH_REVIEW.md (§3 Option B); core/runtime/driver.py; tests/runtime/test_driver_gate_ordering.py.*

## 2026-06-10 — Gate G1 Wave 3B — Gate-ordering review + driver-level characterization (insertion point mapped; orders-vs-positions decided; tests only)
Answered the three Wave-3 implementation preconditions Wave 3A left open and built the one missing test. **(1) Exact Option-B insertion point:** a single line gap in `LoopDriver._run_startup_gate` — between `driver.py:357` (`_check_master_readiness()` returns True) and `driver.py:360` (`_reconcile_ledger()`); the new `_canonicalize_restored_ledger()` call goes there, behind the SAME gating condition as MM.4 (`is_live ∧ has_derivatives ∧ master-ready`), so it runs on FRESH/WARN, never on BLOCK, and is a no-op for paper/replay/equity. Not at `_replay_state` (pre-gate → unverified master, ADR-003/H4) and not after reconcile (MM.4 Decision 2). **(2) Ordering proven:** `_replay_state` (handler construction, `handler.py:186-187`) → RECOVERY_STARTED → RECOVERY_COMPLETED (driver reuses, never re-runs — ADR-001) → master-readiness (`:357`) → reconciliation (`:360`) → RUNNING. Key finding: **FRESH emits no journal event**, so the hook's slot is invisible to the journal and can only be pinned at the CALL level — exactly why Wave 3A §7 gap 1 deferred this test (the existing `test_master_check_runs_before_reconciliation` covers the WARN journal path only). **(3) Decision — SEPARATELY:** restored orders (#8) and restored positions (#7-as-restored) migrate as two independently-revertible commits, in either order — they reconstruct via independent mechanisms on independent objects (`order_repository.py:60`→`NormalizedOrder.instrument` keyed by `correlation_id`; fill-replay `position_tracker.py:31`→`Position.instrument` keyed by `symbol`), and the only intervening gate, reconciliation, is symbol-keyed and identity-blind (`reconciliation.py:39-85` reads `position_tracker._positions` by raw symbol, never orders, never instrument fields). The **position** identity is the corruption-relevant half (greeks/margin consume `position.instrument` — `portfolio_greeks.py:48,56,73`), the restored **order** instrument is largely inert post-restore (groups/idempotency read `correlation_id`/`signal_id`, orders aren't re-sent). **Both** are required for Section-6 closure (#3 spans orders+positions); the interim order-vs-position type mismatch between the two commits is **latent, not active** (no consumer cross-checks them) and must be named in the Wave-3 KB sync. New `tests/runtime/test_driver_gate_ordering.py` (**5 tests, green on first run** — they encode current behavior; **zero production code changed**): a shared call-recorder spans both gate steps (master-readiness appends `READINESS`, wrapped reconcile appends `RECONCILE`) so `seq` IS the interleaving the hook inserts into — FRESH `["READINESS","RECONCILE"]` (hook at index 1), FRESH journal gap, WARN same call order, BLOCK `["READINESS"]` (reconcile + hook unreached, STOPPED, 0 bars), no-checker `["RECONCILE"]`. **Gate G1 remains OPEN; Wave 3 (#2) / Wave 4 (#4) NOT STARTED** — this review only mapped the insertion point, proved the ordering, decided the orders-vs-positions question, and built the slot's tripwire.

Validation:
```
393 passed
0 failed
5 new tests
```
*Ref: docs/reports/G1_WAVE3B_GATE_ORDERING_REVIEW.md; docs/reports/G1_WAVE3A_CHARACTERIZATION_REPORT.md; docs/reports/G1_WAVE3_RESTORE_REVIEW.md (§4 gap 4, §6); docs/reports/SOLE_IDENTITY_PATH_REVIEW.md (§3 Option B, §6); tests/runtime/test_driver_gate_ordering.py.*

## 2026-06-10 — Gate G1 Wave 3A — Restore characterization expansion (H1/H2/H3 pinned; tests only)
Built the characterization safety net for Migration Target **#2 (Restore Path)** before any migration — the F-PARSE-1 discipline applied to the restore defects surveyed in `G1_WAVE3_RESTORE_REVIEW.md`. New `tests/execution/test_g1_restore_characterization.py` (**9 tests, all green on first run** — they encode current behavior; **zero production code changed**). **Group A — H1 pinned:** a futures order built `FUTURE` on the forward path (Wave 2 #1) restores as **`EQUITY`** (`order_repository.py:60` re-parses the symbol; `InstrumentParser.parse` has no Future branch); symbol/side/qty/order_type/signal_id preserved byte-for-byte — the EQUITY assertion is the intentional red→green tripwire for the Option-B post-gate pass. **Group B — H2 pinned:** an option order built with master-resolved `lot_size=65` (#4 selector, F4) restores with the parser's hardcoded **`lot_size=1`** (`instrument_parser.py:39`), multiplier 1.0, type still OPTION, persisted quantity unaffected. **Group C — H3 pinned (safety property):** reconciliation keys on the **raw symbol string** only — the EQUITY-mistyped future and lot-drifted option both reconcile PASS, and a differently-formatted broker symbol (`NSE_FO|NIFTY26JUNFUT`) fails to match on both sides (QUANTITY_MISMATCH + ORPHANED_BROKER_POSITION) — so the Wave 3 pass must preserve `.symbol` byte-for-byte. **Plus:** a schema tripwire (orders/fills/positions persist only `symbol`; red if a structural identity column appears) and an **H6 guard** (a corrupted `positions` snapshot row is invisible to restore — positions rebuild from fill replay only; `position_repository.load_all` stays dead, ADR-001). **New finding:** position identity is parse-built on **both** paths (site #7 forward too) — forward futures positions are already Equity-typed, forward option positions already lot=1; the H1/H2 divergence is **order-level**, and the #6/#7-vs-restore sequencing needs an owner decision before Wave 3 (report §6/§8). Remaining gaps: driver post-gate-ordering test (belongs to the Wave 3 hook step), H7 idempotency-under-swap, H8 group restore, SHORT-side derivatives. **Gate G1 remains OPEN; Wave 3 (#2) / Wave 4 (#4) NOT STARTED.**

Validation:
```
388 passed
0 failed
9 new tests
```
*Ref: docs/reports/G1_WAVE3A_CHARACTERIZATION_REPORT.md; docs/reports/G1_WAVE3_RESTORE_REVIEW.md; docs/reports/SOLE_IDENTITY_PATH_REVIEW.md (§3 Option B, §4); tests/execution/test_g1_restore_characterization.py.*

## 2026-06-09 — Gate G1 Wave 2 — Migration Target #1 (Future Resolution Path) completed
Resolved **F-PARSE-1** (surfaced by the Wave 2A characterization suite): the `ExecutionHandler.process_signal` non-option branch built every futures-style symbol as an **Equity** because `InstrumentParser.parse` has only an Option regex + an Equity fallback — **no Future branch**. **Added a canonical futures resolution path** (`core/execution/futures.py` — module-level `resolve_future(symbol, timestamp, resolver=None) -> Optional[Future]`): regex-detects an NSE monthly future symbol, resolves a `CanonicalInstrument` via `InstrumentResolver.resolve_future(underlying, as_of)`, and **derives a legacy `Future`** from it (`symbol` preserved byte-identical, `multiplier`/`expiry` canonical-sourced). The handler's non-option `else` branch (`handler.py:513`, **3-line edit, site #1 only**) now calls `resolve_future` first and falls through to the unchanged `InstrumentParser.parse` when the symbol is not a future. **Futures no longer misclassify as Equity** — `NIFTY26JUNFUT` now types `FUTURE` (canonical lot 65, expiry 2026-06-30) while `symbol`/`side`/`quantity`/`order_type` stay byte-identical. **`CanonicalInstrument` stays internal** — only its economic facts are read; it never reaches the broker payload, persistence, restore, or reconciliation. Determinism (ADR-003): the FUTURE type keys on the symbol shape, not master presence — a master-absent resolve still derives a `Future` (no type flip on DB presence). **Characterization suite updated:** `test_build_order_futures_currently_falls_back_to_equity` assertion flipped `EQUITY → FUTURE` (name + every symbol/side/quantity/order_type assertion preserved); +3 new unit tests (`tests/execution/test_futures_resolution.py`). **Contracts unchanged — broker payload, persistence, restore, reconciliation** (the `orders` table has no `instrument_type` column, so the FUTURE correction has zero persistence footprint; the value change is not a format change). **Scope: site #1 ONLY** — Target #2 (restore), Target #4 (option), F3, F4, MM.7 wiring untouched; **Gate G1 remains OPEN**.

Validation:
```
379 passed
0 failed
```
*Ref: docs/reports/G1_WAVE2_IMPLEMENTATION_REPORT.md; docs/reports/G1_WAVE2A_BROKER_PAYLOAD_REVIEW.md; docs/reports/SOLE_IDENTITY_PATH_REVIEW.md (Wave 2 #1); docs/ARCHITECTURE_DECISIONS.md (ADR-G1-W2); core/execution/futures.py; core/execution/handler.py.*

## 2026-06-09 — Gate G1 survey: F4 impact CORRECTION — selector silently flipped option lot 75→65 on materialization
First finding of the Gate G1 site survey, and a **correction of the MM.5 record.** The MM.5 KB sync called F4 (NIFTY lot 65 vs assumed 75) "no live impact today (canonical layer dormant)." **That reasoning was wrong.** The `OptionsContractSelector` (`selector.py:108-113`, wired in 4C.5) sources `lot_size` from the resolver; it was "DORMANT" only because the master DB was absent (`resolve_option`→`None`→`INDEX_LOT_SIZES`=75). **MM.5 materialized the master, so the selector is now active:** verified `OptionsContractSelector().select("NSE_INDEX|Nifty 50", …) → LOT=65` (`normalize_underlying("NSE_INDEX|Nifty 50")=="NIFTY"` matches the master `name`; `resolve_option` returns a `CanonicalInstrument` with lot 65), silently overriding the 75 fallback. **Mitigant:** the selector's only caller is `handler.process_signal`'s option branch, whose sole runtime caller is the `LoopDriver` (ADR-006), which has **no live entry script** — so **no order is sized with 65 yet**. **But** the instant the F&O entry script (Planned #4) exists, the live option path sizes with the **unverified** 65 → **F4 verification (65/30 vs the exchange) is now a hard precondition for the live option path**, not a someday-investigation. F3 (tick) is unaffected (the selector reads `ci.lot_size` only). No code changed — survey finding only; F4 record corrected in PROJECT_STATE.
*Ref: docs/PROJECT_STATE.md (Open Findings F4); docs/reports/PHASE_4C_WIRING_REVIEW.md §1 (the "WIRED but DORMANT" row this corrects); core/execution/options/selector.py:108-113.*

## 2026-06-09 — Phase MM.7 — Production Master-Readiness Checker (factory) — blocker #4 BUILT, not yet wired
Built the production construction the MM.4 startup gate was designed to consume, replacing the test-injected verdict lambda with real resolver-backed wiring. New `core/instruments/master_readiness.build_master_readiness(underlyings, *, db_path=None, as_of=None) -> Callable[[], ReadinessVerdict]`: constructs the real `InstrumentResolver` once and returns the zero-arg `() -> assess(resolver, underlyings)` callable the `LoopDriver(master_readiness=...)` gate accepts. `underlyings` is an **explicit parameter** — deliberately *not* auto-derived from `config.symbols` (those are broker keys like `NSE_FO|54710`; deriving an underlying token would itself require resolving the master — bootstrap-fragile — and YAGNI until the sole caller exists). Evaluation only — never downloads/refreshes/repairs the master (Decision 6); `InstrumentResolver` is **lazy-imported** inside the factory so the driver's type-only import of this module stays duckdb-free (no new import weight on the runtime). **No order-routing, reconciliation, or canonical-migration changes** (per the ratified MM.7 scope). **Proven end-to-end (not just unit-tested):** a new `LoopDriver` integration test injects `build_master_readiness(["NIFTY"], db_path=<tmp_path fixture built through the real ingest>)` into a LIVE + derivative driver and asserts the startup gate proceeds **FRESH through the real resolver** — exercising resolver → checker → gate, which the prior lambda injection never did (fixture, not the production DB, so the test stays deterministic and environment-independent). TDD (RED→GREEN): **+4 tests** — `tests/instruments/test_master_readiness.py` ×3 (FRESH against a real master; BLOCK(absent) on a missing DB; the `underlyings` arg honoured — NIFTY-only master is FRESH for `["NIFTY"]` but BLOCK(coverage) for `["NIFTY","BANKNIFTY"]`) + `tests/runtime/test_driver_master_readiness.py` ×1 (the integration). Full suite **365 → 369 passing**, no regressions. **Honesty — blocker #4 is BUILT, NOT CLOSED:** #4's evidence is "*not wired into a live F&O entry script*," and **no live entry script exists** — `LoopDriver(...)` is constructed only under `tests/runtime/` (verified: nothing in `core/`/`scripts/`/`flask_app/` constructs or imports it for production; no `scripts/*runner*.py`). MM.7 delivers the **construction** (the part buildable in-scope); the **live call-site** that invokes it belongs to the F&O runtime entry script (PROJECT_STATE Planned #4: provider/execution/order routing), which the MM scope excludes. So #4 advances to "production checker BUILT + proven, live invocation pending the F&O entry script" — and **G1 is not the sole remaining readiness track** (#4's call-site is a new dependency). **Operational:** ran one authorized manual refresh (`scripts/fetch_instrument_master.py`) — published the 2026-06-09 snapshot (66,107 rows, exit 0) via the MM.6 validate-before-publish path, clearing the WARN to FRESH; the OS scheduled task is intentionally not yet installed. **Reassessment pending owner review** (does the proven factory satisfy MM.7 intent with the live call-site tracked separately, or re-scope?).
*Ref: docs/reports/MM.6_REFRESH_JOB_PLAN.md (§11); docs/reports/PHASE_4C_7_READINESS.md (blocker #4); docs/PROJECT_STATE.md; docs/DRIVER_SPECIFICATION.md §11; ADR-006.*

## 2026-06-09 — Phase MM.6 — Instrument Master Refresh Job — 4C.7 blocker #3 (refresh mechanism) CLOSED
Built the validated, fail-safe daily refresh the materialization policy designed (`MASTER_MATERIALIZATION_POLICY.md` §1/§2/§8), extending `scripts/fetch_instrument_master.py` (no new module). **Validate-before-publish (Option A — stage → validate → promote, owner-ratified):** new `validate_and_publish()` writes parsed rows to a throwaway staging DuckDB, validates coverage through the **same** `master_readiness.assess()` path the startup gate uses, and **only on success** promotes them to the production DB — so a bad/incomplete download **never replaces** the prior good snapshot (the live path degrades to WARN, not BLOCK). **Contract-shape guard** (`_contract_shape_ok`) refuses a 0-OPTION-rows upstream schema shift (derivative segments must be cleanly typed CE/PE/FUT with options present) before publish. **IST-correct stamping** (`ist_snapshot_date`) fixes MM.5 operational finding #1 — `snapshot_date` is stamped in IST (`MarketHours.to_ist`), never machine-local `date.today()`, so materialization and the IST-based gate agree by construction and a fresh download can't manufacture spurious staleness. **Transactional `write_snapshot`** — the per-snapshot DELETE+INSERT is wrapped in one transaction (policy §8#3); a failed INSERT rolls back the DELETE, never leaving a date's snapshot empty. **`run_refresh()`** is the scheduled-job entry point: trading-day guard (self-skips non-trading days/holidays via `MarketHours.is_trading_day`) → download → validate → publish, returning an exit code (0 ok/skip · 1 download error · 2 empty parse · 3 coverage/shape fail) — the non-zero exit is the minimal alerting hook policy §8 flagged as missing, which the OS scheduler surfaces. The OAuth-callback `refresh()` backstop is retained (no-arg → int contract preserved; `flask_app/blueprints/ops/routes.py` unchanged). **Generic underlyings parameter** (`DEFAULT_UNDERLYINGS=("NIFTY","BANKNIFTY")`, not hardcoded in the validation logic). **Docstring corrections (ratified):** dropped the misleading "after OAuth login" from `fetch_instrument_master.py` (policy §1); changed `master_readiness.assess()` "active **weekly** expiry" → "active expiry (≥ expected), any cadence (BankNifty is monthly-only)" so a future maintainer does not accidentally encode a weekly-expiry requirement. **OS-scheduler invocation** (Windows `schtasks` + cron) delivered as a documented artifact (`MM.6_REFRESH_JOB_PLAN.md` §11) — **no daemon/framework/in-repo scheduler** (constraint). **Pre-work — expiry-coverage clarification review: Outcome A, no F5** — traced `assess` → `active_expiry_present` (resolver.py:157–177): the only expiry predicate is `expiry >= expected`, structure-agnostic; "weekly" was never an encoded rule (proven against the materialized master — BankNifty is monthly-only, nearest expiry 2026-06-30, and coverage still passes). TDD (RED→GREEN): **+13 tests** — `tests/instruments/test_master_refresh.py` (12: staging-validate-promote publish/refuse, IST stamping vs UTC boundary, shape guard for stray-type + missing-options, transactional rollback via PK-violation-after-DELETE, trading-day skip, download-error + coverage-fail preserve-prior + non-zero exit) + a readiness regression guard (monthly-only BankNifty passes coverage, locking Outcome A). Full suite **352 → 365 passing**, no regressions. **Caveat (mechanism built, not yet deployed):** the refresh *mechanism* is implemented + validated + tested; **installing the OS scheduled task in the target environment and confirming it fires is the remaining operational step** — blocker #3 CLOSED records the mechanism, not a running job (the MM.4 "built, not yet live-wired" honesty). **Post-MM.6 board:** Gate G1 OPEN · Master DB CLOSED · Refresh Job CLOSED · Production Checker OPEN. **Next: MM.7 (production checker wiring).**
*Ref: docs/reports/MM.6_REFRESH_JOB_PLAN.md; docs/reports/MASTER_MATERIALIZATION_POLICY.md §1/§2/§8; docs/reports/PHASE_4C_7_READINESS.md (blocker #3); docs/PROJECT_STATE.md.*

## 2026-06-09 — Phase MM.5 — Instrument Master Materialization (first production snapshot) — 4C.7 blocker #2 CLOSED
Materialized and validated the **first real instrument master**, closing the 4C.7-readiness blocker the materialization policy could only design around (master DB absent on disk). `scripts/mm5_materialization_check.py` (on-demand acceptance check; uncommitted pending review) downloaded the live Upstox CDN **once** (`complete.json.gz`, **no auth**, 135,424 instruments) and wrote a **single snapshot** (`snapshot_date=2026-06-08`, **65,383 rows** — NSE_FO 40,522 / MCX_FO 15,385 / NSE_EQ 9,337 / NSE_INDEX 139; 22 MB) into `data/instruments/nse_fo_instruments.duckdb` through the **exact production path** (`parse_instruments` + `write_snapshot`) — the forward-only snapshot series begins here (Finding 1; pre-capture `as_of` accuracy is never recoverable). **The decisive result is the validation, not the row count:** **source-contract** PASS (derivative segments cleanly typed CE 27,533 / PE 27,579 / FUT 795 — the "0 OPTION rows on a schema shift" risk is absent; `trading_symbol` snake_case + ms-int `expiry` confirmed as the live encodings); **coverage** PASS (all 4 segments non-empty, EQ-ISIN 100%, active weekly expiry present for NIFTY + BANKNIFTY, BANKNIFTY underlying token reconciled); **resolver** PASS (NIFTY/BANKNIFTY futures, Nifty 50 index, RELIANCE equity all resolve to `CanonicalInstrument`, none `None`); **startup-gate** FRESH (`assess(resolver, [NIFTY, BANKNIFTY])` → FRESH; absent-path → BLOCK/absent — the gate has teeth). **No code in the frozen areas (routing/resolver/gate/reconciliation) changed** — every validation is a read-only resolver call or a pure `assess()`; only a **data** file was written. Review verdict **APPROVED**; **4C.7 readiness blocker #2 (master DB absent) → CLOSED** (the remaining 4C.7 blockers are Gate G1, the scheduled refresh job, and production checker wiring). Validation surfaced two **attribute** data-quality findings with **no live impact today** (the canonical attribute layer is dormant pre-4C.7), tracked explicitly as **F3 — Tick Size Scaling Defect** (`CanonicalInstrument.tick_size` stored ≈100× too large — raw paise, unnormalized; OPEN DEFECT, owned by the parser/4C.7 slice) and **F4 — Lot Size Discrepancy** (materialized NIFTY=65 / BANKNIFTY=30 vs the repo's prior 75 assumption — INVESTIGATION REQUIRED, verify against exchange circulars/broker master before any change, neither value asserted here). **Next: MM.6 (scheduled refresh job) → MM.7 (production checker wiring).**
*Ref: docs/reports/MM.5_MATERIALIZATION_REPORT.md; docs/reports/PHASE_4C_7_READINESS.md (blocker #2); docs/reports/MASTER_MATERIALIZATION_POLICY.md §3; docs/PROJECT_STATE.md (Open Findings F3/F4); scripts/mm5_materialization_check.py.*

## 2026-06-08 — Phase MM — Instrument Master Startup Readiness Gate (MM.1–MM.4)
Built the staleness + coverage gate the 4C.7 readiness review designed (`docs/reports/MASTER_MATERIALIZATION_POLICY.md` §4/§5), in two commits. **Foundation (MM.1–MM.3, `f8b26cd`):** `InstrumentResolver.latest_snapshot_date()` — the single on-disk freshness fact (`MAX(snapshot_date)`, `None` when absent/empty) every consumer reads instead of re-deriving — and the pure `core/instruments/master_freshness.expected_snapshot_date(now)` (the latest NSE trading day whose **08:30 IST refresh cutoff** has passed, via `MarketHours` IST + NSE holidays). Date primitives only, no startup behavior change; +7 tests. **Gate (MM.4, `db7a7d4`):** master readiness wired into `LoopDriver._run_startup_gate()` **after `RECOVERY_COMPLETED`, before reconciliation** — identity must be trustworthy before positions are matched through it (Decision 2; this overrides the review-request diagram, which had it after reconciliation). New `core/instruments/master_readiness.py` — pure `evaluate(latest, expected, coverage_ok)` → FRESH/WARN/BLOCK with **coverage as the hard gate and date as the early-warning proxy** (precedence absent→coverage→stale→warn→fresh; a today-dated but empty/partial master BLOCKs regardless of date — the schema-shift risk a date-only check misses) + `assess(resolver, underlyings, now)`. Two read-only **coverage facts** added to `InstrumentResolver` (`segment_row_count`, `active_expiry_present`, scoped to the latest snapshot — facts only, the verdict lives in the readiness module, Decision 5). `has_derivatives()` segment-prefix scope helper (`core/runtime/instrument_scope.py`) is **master-independent** — master absence is itself a BLOCK condition, so scope cannot depend on resolving the master (the chicken-and-egg the design review flagged; Decision 4). Two new journal events (`core/runtime/event_journal.py`): `INSTRUMENT_MASTER_UNAVAILABLE` (CRITICAL — BLOCK → `abort_startup()` → STOPPED + `alerter.critical`, mirroring `RECONCILIATION_FAIL`; `reason` ∈ {absent, coverage, stale} in metadata) and `INSTRUMENT_MASTER_STALE` (WARNING — a 1-day-stale start journaled + `alerter.warning`, durable because telemetry is lossy; Decision 3). Enforced **only** on LIVE + derivative universe + an injected checker; **equity-only LIVE, paper, and REPLAY bypass — zero behavior change off the live-F&O path**. **Evaluation only** — the gate is handed a verdict and never downloads/refreshes/retries/repairs the master (Decision 6); materialization stays a separate responsibility. The checker is a zero-arg callable injected like `broker_positions` (the driver never imports the resolver). Owner Decisions 1–6 and the full design critique are archived in `docs/reports/MM.4_DESIGN_REVIEW.md`. TDD (RED→GREEN): **+33 tests** — 7 scope (`tests/runtime/test_instrument_scope.py`), 16 readiness + coverage facts (`tests/instruments/test_master_readiness.py`, incl. the headline BLOCK(coverage) proof on a today-dated empty master), 8 driver-gate (`tests/runtime/test_driver_master_readiness.py`, incl. the before-reconciliation ordering proof and reconcile-not-called-on-BLOCK), +2 event-severity cases. Full suite **319 → 352 passing**, no regressions. **Not yet live:** the production `master_readiness` callable (resolver + traded underlyings → `assess`) is injected only in tests — live F&O entry-script wiring lands with the F&O runtime slice; until then live-F&O-without-a-checker is a vacuous pass (mirrors the deferred-reconciliation contract).
*Ref: docs/reports/MM.4_DESIGN_REVIEW.md; docs/reports/MASTER_MATERIALIZATION_POLICY.md §3–§5; docs/reports/PHASE_4C_7_READINESS.md; ADR-001/003/006; docs/PROJECT_STATE.md.*

## 2026-06-08 — Phase 4C.7 readiness: materialization & staleness policy (design) + NO-GO verdict
**Design-only (no code).** Produced two governance docs. `docs/reports/MASTER_MATERIALIZATION_POLICY.md` — the **PROPOSED** policy making the instrument master a content-verified production dependency: refresh **source** (public no-auth CDN, decoupled from the OAuth session); **cadence** (OS-scheduled daily job @ 08:30 IST + OAuth-callback backstop — no in-repo scheduler exists); a **centralized freshness model** (`latest_snapshot_date()` + `expected_snapshot_date` via `MarketHours` IST/NSE-holidays) plus **coverage assertions** (segments non-empty, EQ ISIN, active weekly expiry per underlying — age alone is a coarse proxy); **FRESH/WARN/BLOCK** thresholds (coverage is the hard gate); **fail-fast startup** (new `EventType.INSTRUMENT_MASTER_UNAVAILABLE` + a check in `_run_startup_gate()`, scoped to LIVE + F&O; equity/paper/replay keep the soft fallback); **replay** (offline, `as_of`, forward-only Finding-1 caveat); **CI/test** (fixtures via the real ingest pipeline + a PROPOSED source-contract test for upstream schema drift); and a **failure-mode** table. `docs/reports/PHASE_4C_7_READINESS.md` — Go/No-Go = **NO-GO**, on two objective, checkable conditions: **Gate G1 not closed** (`SOLE_IDENTITY_PATH_REVIEW.md` absent; the 11 legacy order-identity sites remain — **untouched** by 4C.6/Finding 2, which were greeks + chain-discovery) and the **master DB absent on disk** (`data/instruments/**` empty); the staleness gate is designed-not-built. Finding 2 is ✓. Includes an ordered path-to-GO. Nothing implemented; design only.
*Ref: docs/reports/MASTER_MATERIALIZATION_POLICY.md; docs/reports/PHASE_4C_7_READINESS.md; docs/reports/MASTER_MATERIALIZATION_READINESS.md; docs/reports/PHASE_4C_IMPLEMENTATION_PLAN.md §2/§6.2; docs/PROJECT_STATE.md.*

## 2026-06-08 — Finding 2 remediation — OptionsProvider snapshot-aware reads (4C.7 unblock)
Remediated **Finding 2** from the materialization readiness review (the highest-risk item; a 4C.7 hard blocker). `core/data/options_provider.py` was a **second, snapshot-blind reader** of the instrument master file — `get_weekly_expiry`/`get_available_strikes`/`get_lot_size`/`get_expiry_list` queried `instruments` by raw SQL with no `snapshot_date` filter, so once daily snapshots accumulate `DISTINCT strike/expiry` returns a cross-snapshot **UNION** and `lot_size … LIMIT 1` an **arbitrary** snapshot's lot. Each query now scopes to the latest snapshot via `AND snapshot_date = (SELECT MAX(snapshot_date) FROM instruments)` — a **no-op at one snapshot** (zero behavior change today), correct once snapshots accumulate. Chain-**discovery** reads (list expiries/strikes) stay in `options_provider` rather than routing through `InstrumentResolver` (which exposes point identity resolution, not discovery), per the readiness decision. Method signatures unchanged; the three consumers (`flask_app/blueprints/options.py`, `app_facade/options_facade.py`, `core/analytics/options_analytics.py`) are unaffected. TDD (RED→GREEN): **+4 tests** (`tests/data/test_options_provider_snapshot.py`) — a stale-OLD + NEW two-snapshot master (built through the real 4C.1 ingest) proves lot/strike/expiry/weekly-expiry all return the **latest snapshot only**; all 4 RED before the fix (lot returned stale 50, expiries/strikes unioned, weekly-expiry picked the stale 2026-03-25). Full suite **308 → 312 passing**, no regressions. **4C.7 blocker cleared;** remaining 4C.7 preconditions: materialized master + staleness policy + Gate G1 report. **Repo-coherence fix (surfaced while committing this):** the unanchored `.gitignore` `data/` rule had silently kept the **entire `core/data/` source tree (15 modules) untracked** even though tracked code imports `core.data.options_provider` (`options_facade`, `options_analytics`, flask options blueprint) — a fresh clone would `ImportError`. Anchored to `/data/` (top-level market-data store stays ignored) and **tracked core/data/ + tests/data/** (16 source files + 1 test, no data artifacts) — so the Finding 2 fix in `options_provider.py` is now actually in git, and the clone-breaking defect is closed.
*Ref: docs/reports/MASTER_MATERIALIZATION_READINESS.md (Finding 2 / Decision 2); docs/reports/PHASE_4C_IMPLEMENTATION_PLAN.md §6.2; docs/PROJECT_STATE.md.*

## 2026-06-08 — Master Materialization Readiness Review + Phase 4C.6 (greeks asset_class dispatch)
Inserted a **Master Materialization Readiness Review** between 4C.5/Gate-G1 and 4C.6–4C.8 (`docs/reports/MASTER_MATERIALIZATION_READINESS.md`) to answer *how the Instrument Master becomes a reliable production dependency*. Verdict: **architecture materialization-ready, data not.** Surfaced two findings the 4C.5 wiring review missed (it grepped the `InstrumentMaster` class, not the DB **file**): **(F1)** `scripts/fetch_instrument_master.py` fetches only the current `complete.json.gz` (no dated endpoint), so the snapshot series is **forward-only from first capture** — pre-capture `as_of` resolves to the earliest snapshot with a logged warning (the 2024 SEBI 50/75 backtest gets today's lot), un-backfillable from the live source; **(F2)** `core/data/options_provider.py` is a **second, snapshot-blind reader** of `data/instruments/nse_fo_instruments.duckdb` (raw SQL, no `snapshot_date` filter on `get_weekly_expiry`/strikes/lot_size/expiry-list) — harmless at one snapshot, returns an arbitrary snapshot's lot once snapshots accumulate. The 7 readiness questions (source/refresh/failure/dependency/offline-replay/historical-pin/CI-fixtures) answered file:line-anchored; source is a **public, no-auth CDN** (the docstring's "after OAuth" is misleading); refresh trigger today is the **OAuth callback only** (`flask_app/blueprints/ops/routes.py:179-185`), no scheduler. **Owner-ratified decisions (locked, `PHASE_4C_IMPLEMENTATION_PLAN.md` §6.2):** proceed to 4C.6 (not DB-gated); **F2 = HARD BLOCKER for 4C.7**; **F1 = documented limitation**; **4C.7 precondition = materialized master + staleness policy + fail-fast on the live F&O path** (in addition to Gate G1). **Phase 4C.6** (TDD, RED→GREEN): `GreeksCalculator.calculate` dispatches on `asset_class` instead of `isinstance(Equity/Future/Option)` (`core/risk/greeks/greeks_calculator.py`); a module helper `_asset_class()` reads `CanonicalInstrument.asset_class` or normalises a legacy `Instrument.type` (InstrumentType names align with AssetClass) — single-file change (no `instrument_base` edit; `greeks_calculator → canonical` import only, no cycle), closing the legacy subclass-tree dispatch. Legacy Greeks numbers **unchanged** (parity-asserted); the canonical branch is **dormant until canonical flows at 4C.7**. +8 tests (`tests/risk/test_greeks_asset_class_dispatch.py` — 3 legacy parity, 4 canonical dispatch, 1 ast source-guard); full suite **300 → 308 passing**, no regressions.
*Ref: docs/reports/MASTER_MATERIALIZATION_READINESS.md; docs/reports/PHASE_4C_IMPLEMENTATION_PLAN.md §4C.6/§6.2; docs/reports/PHASE_4C_WIRING_REVIEW.md; ADR-001/002/003; docs/PROJECT_STATE.md.*

## 2026-06-08 — Phase 4C Core Instrument Architecture (4C.1–4C.5)
Introduced the platform-owned **canonical instrument identity** the F&O pillars (Planned #4/#5/#6) sit on, as pure additions + one behavior-preserving build-seam adapt — the canonical layer is **unwired into the runtime**, so live behavior is unchanged. **4C.1** extended the master SSOT (`scripts/fetch_instrument_master.py`): ingest **NSE_EQ + NSE_INDEX** (equity/index first-class), add `isin`/`tick_size`, and convert to a **snapshot time-series** (`PRIMARY KEY (instrument_key, snapshot_date)`) for as_of resolution; `strike`/`tick_size` widened to `DOUBLE`. **4C.2** `CanonicalInstrument` — one immutable `asset_class`-discriminated value object (`multiplier == lot_size`; INDEX non-tradable) + deterministic broker-independent `canonical_id` and `normalize_underlying` (`core/instruments/canonical.py`, `identity.py`). **4C.3** `InstrumentResolver` — the **sole reader** of the master, as_of-aware, cached, with an **observable** warning when as_of precedes all snapshots (Constitution §6 / ADR-004 — not silent) (`core/instruments/resolver.py`). **4C.4** `BrokerMapping` interface + `UpstoxMapping` (master projection; round-trip identity proven; `MIS/CNC/NRML`→Upstox product translation) (`core/brokers/mapping/`); added `resolver.resolve_by_instrument_key`. **4C.5** `OptionsContractSelector` sources `lot_size` from the resolver, **byte-for-byte legacy fallback** when the master is absent and still returns a legacy `Option` — the output type never flips on DB presence (ADR-003). Decisions locked in `PHASE_4C_IMPLEMENTATION_PLAN.md`: **Review Gate G1** (sole-identity-path proof, blocks 4C.7), `InstrumentMaster` → **deprecate not repair**, snapshot retention → **document not solve**. `PHASE_4C_WIRING_REVIEW.md` answers Gate G1 honestly — **today "Yes, legacy construction remains"**; materializing the master DB is the unblocker. TDD (RED→GREEN): **+49 tests** (ingest/canonical/identity/resolver/mapping/selector); full suite **251 → 300 passing**, no regressions.
*Ref: docs/reports/CANONICAL_INSTRUMENT_ARCHITECTURE.md; docs/reports/PHASE_4C_IMPLEMENTATION_PLAN.md; docs/reports/PHASE_4C_WIRING_REVIEW.md; docs/reports/FNO_PRODUCT_DISCOVERY.md; ADR-001/002/003/004; docs/PROJECT_STATE.md.*

## 2026-06-07 — LoopDriver Phase G — COMPLETE (per-signal isolation + IN-001 kill-switch single source)
Closed the two remaining Phase G increments; the deterministic LoopDriver is now feature-complete (A–H + the Phase G closers). **(a) §8.4 per-signal exception isolation** — each `process_signal` in `LoopDriver._dispatch_signals` is wrapped so one signal's failure cannot kill the loop: the failure is logged, journaled `BROKER_ERROR` (WARNING, §15.4; metadata `error`/`signal_id` — the signal's explicit id or a composed `strategy_id:symbol:timestamp`, not the handler's internal derivation), surfaced as an edge-triggered telemetry log line, and routing continues to the next signal/bar (Constitution §6 — logged, never swallowed). `EXECUTION_CALLS` is metered only on a successful call. New `RuntimeTelemetryPublisher.publish_log(level, message)` added to the bridge + `TelemetryTransport` Protocol (the real ZMQ transport already satisfies it; `telemetry.logs.{node}`). **(b) IN-001 `KILL_SWITCH_ACTIVATED` single source** — emission moved from the watchdog **data-health proxy** to a single observation of the handler's own kill-switch edge (`_kill_switched` False→True, §10.7) in the new `LoopDriver._check_kill_switch`, run each tick after routing and the watchdog drive. Every cause — drawdown / broker / daily-trade-limit **and** stale data — is now journaled exactly once, in replay/paper as well as live; the proxy emission (and its `KILL_SWITCH_EVENTS` meter) was removed from `_drive_watchdog`, while `WATCHDOG_STALE_DATA` stays there. Edge-triggered via a `_kill_switch_was_active` latch. The real watchdog trips the kill switch via `handler.activate_kill_switch()` (`watchdog.py:82`); the stale-data tests were rewired so `FakeWatchdog` shares the driver's handler and trips it the same way (preserving `WATCHDOG_STALE_DATA` → `KILL_SWITCH_ACTIVATED` ordering). **IN-001 is Resolved** (`ARCHITECTURE_DECISIONS.md`). TDD (RED→GREEN): +17 tests — 3 bridge `publish_log`, 8 §8.4 (`tests/runtime/test_driver_broker_error.py`), 6 IN-001 (`tests/runtime/test_driver_kill_switch.py`); full suite **234 → 251 passing**, no regressions.
*Ref: docs/DRIVER_SPECIFICATION.md §8.4, §10.7; ARCHITECTURE_DECISIONS.md IN-001; ADR-001/004/005/006; docs/PROJECT_STATE.md.*

## 2026-06-07 — LoopDriver Phase H.3 — Position Publishing (ZMQ, §10.4)
`RuntimeTelemetryPublisher.publish_positions(data)` + `LoopDriver._build_positions()` publish a positions snapshot over the existing ZMQ transport on `telemetry.positions.{node}`, on the **same** telemetry cadence as metrics/health (§10.2) and best-effort (§10.6) — closing the **last §10 telemetry gap** (H.1 metrics + H.2 health already complete; **§10 telemetry is now COMPLETE**). The payload is a **read-only projection** of ledger truth (`execution.position_tracker.get_all_positions()`, ADR-001), keyed by symbol; each entry is `{quantity, avg_price, side, pnl_pct}` where `pnl_pct` is a documented `0.0` placeholder until live MTM (§10.4 known gap) — it computes **no PnL**. `side` (`pos.side.value`) and `avg_price` (the real `Position` model field) are emitted over §10.4's `.avg_entry_price` wording; with no `ExecutionHandler` the book is empty `{}`. Additive only (new `TelemetryTransport` Protocol method, new bridge method, new `_build_positions`, one line in `_drive_telemetry`); the §10 consumer (`flask_app/telemetry_bridge.py`) is shape-agnostic, so this shape defines the contract. TDD (RED→GREEN): +3 bridge + +8 driver tests (`tests/runtime/test_driver_position_publish.py`); full suite **223 → 234 passing**, no regressions. *(docs/DRIVER_SPECIFICATION.md §10.4; ADR-001/002/003/006)*
*Ref: docs/DRIVER_SPECIFICATION.md §10.4; docs/PROJECT_STATE.md.*

## 2026-06-07 — PortfolioView — read-only portfolio projection (Phase 1)
Added `core/execution/portfolio_view.py`: `PortfolioView` + immutable `PortfolioSnapshot`, the generalization of `ExecutionHandler.get_stats()` into a deterministic read surface over the existing trackers (pillar #2 "Ledger as an explicit surface"; PORTFOLIO_STATE_DISCOVERY.md §5.2). Given `(current_prices, cash_balance)` it returns a snapshot of `positions`, `cash_balance`, `realized_pnl`, `unrealized_pnl`, `mtm_equity` (= cash + unrealized — fixes the §4.2 "equity = cash only" gap; realized is already baked into cash), `gross_exposure`, `used_margin`. Every field is a pass-through of an existing tracker calculation — **no new financial logic, no new store, no second ledger** (ADR-001); it never mutates the trackers. `cash_balance` is caller-supplied from `ExecutionMetrics.cash_balance` (the view does **not** import `ExecutionMetrics`, avoiding coupling to `handler.py`). `daily_pnl` deliberately deferred (out of the phase's narrow scope). Pure infra (`ast` forbidden-import guard, ADR-002); deterministic (ADR-003); read-only and **not yet wired into the runtime** (Phase 2/H.3 are the future consumers). TDD (RED→GREEN): +10 tests (`tests/execution/test_portfolio_view.py`); full suite **213 → 223 passing**, no regressions. *(docs/reports/PORTFOLIO_STATE_DISCOVERY.md §5; ADR-001/002/003)*
*Ref: docs/reports/PORTFOLIO_STATE_DISCOVERY.md; docs/PROJECT_STATE.md.*

## 2026-06-07 — Phase 0 Ledger Hygiene — portfolio defect remediation
Fixed the two confirmed defects from Portfolio State Discovery. (1) `PortfolioGreeks.calculate_portfolio_greeks` iterated `get_all_positions()` dict **keys** (symbol strings) → `AttributeError` on any non-empty book; now iterates `.values()` (`core/risk/greeks/portfolio_greeks.py`). (2) `UpstoxAdapter.get_positions` constructed `Position(avg_entry_price=…, last_update=…)` — kwargs the model rejects → `TypeError` (swallowed → silent `{}`); now uses `avg_price`/`last_updated` and derives `side` (LONG/SHORT/FLAT) from the signed broker quantity with `abs(qty)` (`core/brokers/upstox_adapter.py`). TDD throughout (RED verified before GREEN): +12 tests — `tests/risk/test_portfolio_greeks.py` (5), `tests/brokers/test_upstox_positions.py` (4), `tests/execution/test_reconciliation_broker.py` (3, broker→reconcile end-to-end). Full suite **201 → 213 passing**, zero regressions; both edited methods have **no live caller**, so existing execution/telemetry behavior is unchanged by construction. Scope-limited hygiene — no `PortfolioView`, no H.3, no margin redesign, no persistence change. **Ledger-substrate recommendation (APPROVED ARCHITECTURAL DIRECTION, not implemented):** SQLite `data/execution.db` = canonical execution truth; DuckDB `trading.db` = audit/analytics projection. **Open assumption:** Upstox `/portfolio/net-positions` returns a signed net quantity (negative = short) — to confirm in Planned #6 before live use.
*Ref: docs/reports/PORTFOLIO_STATE_DISCOVERY.md; docs/PROJECT_STATE.md; ADR-001.*

## 2026-06-06 — LoopDriver Phase H.2 — Health Publishing (ZMQ, §10.5)
`RuntimeTelemetryPublisher.publish_health(data)` + `LoopDriver._build_health()` publish a node health/liveness projection over the existing ZMQ transport on `telemetry.health.{node}`, on the **same** telemetry cadence as metrics (§10.2), best-effort (§10.6). Payload (6 fields): `node`, `state` (RuntimeState — covers loop/startup/recovery), `data_healthy` (the watchdog's **public** flag; True when no watchdog), `market_open` (`MarketHours.is_market_open(clock.now())`, keyed to the trade clock — deterministic in replay, wall-clock-correct live), `uptime_s` (wall-clock, §6.5 operational), `last_tick` (trade clock). A **read-only projection** of state the driver owns or reads publicly — it reaches into **no** private execution internals (no `_kill_switched`/`_trades_today`), sidestepping the §10.7 coupling the spec accepts for v1. Reuses the existing bridge/transport/cadence — no second publisher. TDD: +3 bridge + +7 driver tests; runtime suite **191 → 201 passing**. *(docs/DRIVER_SPECIFICATION.md §10.5; Constitution §6; ADR-001/003)*
*Ref: docs/DRIVER_SPECIFICATION.md §10.5; docs/PROJECT_STATE.md.*

## 2026-06-06 — LoopDriver Phase H.1 — Metrics Publishing (ZMQ, §10.2/§10.3)
`RuntimeTelemetryPublisher` (`core/runtime/telemetry_publisher.py`) consumes the Runtime Observability Layer snapshot and publishes it via the existing ZMQ `TelemetryPublisher` on `telemetry.metrics.{node}`, clock-throttled to `telemetry_interval_s` (§10.2), best-effort (§10.6), driver-driven and closed on stop. Payload = the 12 `RuntimeMetric` counters — a deliberate **supersession** of §10.3's `handler.metrics` field list (`cash_balance`/`max_drawdown_pct`/…; no PnL). DI via `typing.Protocol` (`TelemetryTransport` / `SupportsSnapshot`) — the real transport satisfies it structurally (contract-tested, no socket). No duplicate counters, no second telemetry model. TDD: 9 bridge + 7 driver tests; runtime suite **175 → 191 passing**. *(docs/DRIVER_SPECIFICATION.md §10.1–§10.3, §10.6; ADR-001/002/003/006)*
*Ref: docs/DRIVER_SPECIFICATION.md §10.1–§10.3; docs/PROJECT_STATE.md.*

## 2026-06-06 — LoopDriver Runtime Observability Layer (runtime metrics counters)
Added an internal, in-process runtime telemetry layer to the `LoopDriver`: a small, inert-by-default, dependency-injected metric-counter seam (`core/runtime/metrics.py`) that gives visibility into runtime behavior **without changing it**. `RuntimeMetric` enumerates exactly 12 in-scope counters — lifecycle (`startup`/`recovery`/`reconciliation`/`stop`), runtime (`bars_processed`/`signals_received`/`signals_routed`/`loop_iterations`), watchdog (`heartbeats_emitted`/`stale_data_events`/`kill_switch_events`), execution (`execution_calls`); no broker/PnL/portfolio metrics. `TelemetrySink` is the injected interface (`increment(metric, count=1)`); `NullTelemetrySink` is the inert default and `InMemoryTelemetrySink` accumulates counts (`get`/`snapshot`). The driver bumps counters at each existing observable point via a `_meter()` helper that swallows-and-logs any sink failure (`setup_logger("loop_driver")`), so **telemetry is best-effort and can never stop trading**; no control flow branches on telemetry state, so "telemetry never affects decisions" holds structurally. Observation only — never a source of truth, never in the trade decision path (ADR-001), no globals/singletons. `STOP_COUNT` counts only a clean `finalize_stop` (refuse-to-start is not a stop); `RECONCILIATION_COUNT`/`RECOVERY_COUNT` count every cycle (incl. vacuous/fail). **Naming:** this is the **"Runtime Observability Layer"**, recorded as a standalone milestone outside the A–H execution-build sequence — it is deliberately **distinct from** the still-planned §10 ZMQ `TelemetryPublisher` wire transport (the roadmap "Phase H — Telemetry"), and is a **foundation** that future wire publisher can read from and publish. TDD: `tests/runtime/test_telemetry_sink.py` (9) + `tests/runtime/test_driver_telemetry.py` (11); runtime suite **155 → 175 passing** (existing startup-gate / execution-routing / watchdog suites unchanged).
*Ref: docs/DRIVER_SPECIFICATION.md (observability / §10); Constitution Principle 1, §6; ADR-001/002/006; docs/PROJECT_STATE.md.*

## 2026-06-06 — LoopDriver Execution Routing — routing sub-slice (Phase G)
Wired signal routing into the `LoopDriver` (`core/runtime/driver.py` `_dispatch_signals`): each pulled `SignalEvent` is forwarded, in list order, to the canonical `ExecutionHandler.process_signal(signal, current_price=bar.close)` (§8.1). `current_price` is always `bar.close` — deterministic, so live and replay traverse the identical routing path. Routing is gated on handler presence **and** the RUNNING state (PAUSED suspends it without going blind, §3.1); the no-handler path (replay/inert/test) collects only, and `signals_pulled` still counts unconditionally (Phase D preserved). `_tick` is untouched, so watchdog `record_bar`/heartbeat ordering is byte-for-byte unchanged. ADR-006: the driver is the sole runtime caller of `process_signal`; the `SignalSource` seam holds no handler handle (statically guarded). The Phase E/F "routes nothing" AST guard inverted to `test_driver_routes_through_process_signal`; every watchdog behavioral test is unchanged. Deliberately narrow — no orders/fills/sizing/risk/telemetry. Added `tests/runtime/test_driver_execution_routing.py` (11 tests) + a `process_signal` recording spy on `FakeExecutionHandler`; runtime suite **155 passing**. Runtime flow now `Bar → Clock → SignalSource → LoopDriver → ExecutionHandler → RuntimeWatchdog → RuntimeEventJournal`. **Deferred (close Phase G next):** per-signal exception isolation (§8.4, `BROKER_ERROR`) and the IN-001 `KILL_SWITCH_ACTIVATED` single-source migration — now reachable, since `process_signal` can trip the handler's own kill switch while the driver still observes only `watchdog.data_healthy`.
*Ref: docs/DRIVER_SPECIFICATION.md §8; ADR-005, ADR-006; ARCHITECTURE_DECISIONS.md IN-001; docs/PROJECT_STATE.md.*

## 2026-06-06 — LoopDriver Startup Gate / Recovery (Phase F)
Added the §11 startup-validation gate to the `LoopDriver` (`core/runtime/driver.py`), run inside `run()` before the loop when an `ExecutionHandler` is injected. Sequence: `RECOVERY_STARTED` → reuse the handler's already-run recovery (`load_db_state=True`; the driver **never** calls `_replay_state()` — ADR-001) → `RECOVERY_COMPLETED` → reconcile the restored ledger against broker truth (`handler.reconciliation.reconcile(broker_positions)`) → empty = `RECONCILIATION_PASS` → `RUNNING`, non-empty = `RECONCILIATION_FAIL` → `abort_startup()` (STOPPED + critical alert; tick loop never runs). Reconciliation branches by **source presence** (require flag + injected `broker_positions`), not by mode; vacuously clear for paper/replay and for LIVE until the broker book is wired (Planned #6). **LIVE mode now requires an injected `ExecutionHandler`** — `run()` raises `RuntimeError` without one; the no-handler ungated path is replay/inert/test only. Still execution-free — no `process_signal` (ADR-006); the watchdog guard was narrowed from "no `ExecutionHandler` dependency" to the real ADR-006 invariant (no `process_signal` call), allowing the legitimate handler reference the gate requires. Phase E watchdog tests updated to inject a `FakeExecutionHandler`; added `FakeExecutionHandler`/`FakeReconciliation` + `test_driver_startup_gate.py`; runtime suite increased to **144 passing**.
*Ref: docs/DRIVER_SPECIFICATION.md §11; ADR-001, ADR-006; docs/reports/PHASE_F_STARTUP_GATE_PLAN.md; docs/PROJECT_STATE.md.*

## 2026-06-05 — LoopDriver Watchdog Integration (Phase E)
Wired `RuntimeWatchdog` into the `LoopDriver` (`core/runtime/driver.py`; watchdog reused unchanged): per-bar `record_bar()`, per-tick `check_data_staleness()` + `write_heartbeat(bars_processed)` after the symbol sweep, **all live-mode gated** (replay drives none). Heartbeat generation and staleness monitoring are now operationally driven. A stale-feed trip is recorded to the runtime journal edge-triggered as `WATCHDOG_STALE_DATA` + `KILL_SWITCH_ACTIVATED` (observed via the watchdog's public `data_healthy` flag). The loop remains execution-free — no `ExecutionHandler` routing, no `process_signal` (ADR-006). Added `FakeWatchdog` + 11 watchdog tests; runtime suite increased to **135 passing**. Runtime chain: `Bar → Clock → SignalSource → RuntimeWatchdog → RuntimeEventJournal`.
*Ref: docs/DRIVER_SPECIFICATION.md §9; ADR-004; docs/PROJECT_STATE.md.*

## 2026-06-05 — Deterministic LoopDriver build (Phases A–D) + runtime primitives
Built the `core/runtime/` package and the deterministic loop scaffold. Primitives: `SignalSource` (strategy-agnostic pull seam), `DriverConfig` (+ `Mode`), `RuntimeEventJournal` (append-only `logs/runtime_events.jsonl`), `Clock.set_time`. `LoopDriver` phases: A (six-state lifecycle + §3.2 transitions), B (journal emission on transitions, edge-triggered), C (tick loop + clock advancement + market-data pull, replay-exhaustion vs live-poll, `max_bars`, cooperative stop), D (SignalSource pull — signals collected in list order but **not routed**). Strategy-agnostic (`ast` forbidden-import guard); single-threaded deterministic decision path (ADR-003). Execution-free throughout.
*Ref: docs/DRIVER_SPECIFICATION.md; docs/reports/LOOPDRIVER_IMPLEMENTATION_PLAN.md; ADR-002/003/006; docs/PROJECT_STATE.md.*

## 2026-06-04 — Repository knowledge system established
Created the permanent knowledge base under `docs/`: `docs/SESSION_BOOTSTRAP.md`, `docs/PROJECT_STATE.md`, `docs/ARCHITECTURE_DECISIONS.md` (ADR-001…ADR-005), and this changelog.

## 2026-06-04 — RuntimeWatchdog extracted
Extracted the heartbeat generator and data-staleness watchdog from `core/runner.py` into `core/execution/watchdog.py` (`RuntimeWatchdog`). Strategy-free; preserves `logs/heartbeat.json` keys byte-for-byte; staleness trips `ExecutionHandler.activate_kill_switch` during market hours. Both paths smoke-tested. Passive by design — not yet driven by a loop.
*Ref: docs/PLATFORM_INVENTORY.md (Observability); docs/reports/RUNNER_EXTRACTION_BLUEPRINT.md.*

## 2026-06-04 — Platform Inventory completed
Classified all 125 surviving modules into single categories with KEEP/REFACTOR/REMOVE verdicts and a constitution cross-analysis. Verified zero Platform→Strategy imports; identified seven dead `core/data/*` twins (0 importers), soft strategy residue, and the missing deterministic loop driver as the top pillar.
*Ref: docs/PLATFORM_INVENTORY.md.*

## 2026-06-04 — Platform Constitution created
Authored `docs/PLATFORM_CONSTITUTION.md` v1.0 — mission, five core principles (Ledger Is Truth; Execution Before Alpha; Deterministic Operation; Risk Before Trading; No Trading On Stale Data), platform responsibilities, explicit non-responsibilities, and the strategy boundary.
*Ref: docs/PLATFORM_CONSTITUTION.md.*

## 2026-06-04 — Runner capability review, dependency analysis, and extraction blueprint produced
Reviewed the five surviving platform capabilities (Trade Learning Protocol, ExecutionHandler, heartbeat, ZMQ telemetry, deterministic runner); performed an import-level dependency analysis of `core/runner.py`; produced a per-capability extraction blueprint with difficulty ratings and hidden risks, plus a readiness assessment for a futures + option-selling core.
*Ref: docs/reports/CAPABILITY_REVIEW.md; docs/reports/RUNNER_DEPENDENCY_ANALYSIS.md; docs/reports/RUNNER_EXTRACTION_BLUEPRINT.md.*

## 2026-06-04 — ExecutionHandler restored
Restored `core/execution/handler.py` (842-line OMS/EMS core) and its infra dependencies (`capture.py`, `diagnostic_engine.py`, `metrics_service.py`) that the initial migration had dropped; removed the wrongly-retained strategy module `pixityAI_risk_engine.py`. Fixed dead navigation links in `flask_app/templates/base.html`. Verified the Flask app boots with 5 infra blueprints and `/options/`, `/database/` render 200.
*Ref: docs/reports/SALVAGE_REPORT.md §8.*

## 2026-06-04 — Salvage migration concluded
Concluded the infra-only migration of the Upstox bot (Nifty / equity / options) from `D:\BOT\root` into `F:\Nifty`. Added the options-dashboard + Flask + facade + scripts tiers; removed FTMO, strategy docs, and orphaned/broken `core/data` analytics plumbing; rewrote `requirements.txt` from an AST scan of actual imports. Full `core` + `app_facade` import-walk clean; forbidden-import scan (strategies/indicators/runner/backtest/state/models/ftmo) empty.
*Ref: docs/reports/SALVAGE_REPORT.md.*
