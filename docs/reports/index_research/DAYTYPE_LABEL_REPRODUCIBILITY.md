# DayType — Is the Label Generator Reproducible?

Generated by `scripts/daytype/verify_label_reproducibility.py`. Read-only against `data/features/day_type/`; the stored labels are a production model's training target and this run cannot move them.

Answers the empirical half of Finding B in `REGIME_DETECTION_SPEC_AUDIT_2026-09-09.md`. Predictions P1-P4 were pinned in the script docstring before the run.

Stored labels: **3,413 sessions**, 2012-01-04 to 2025-12-31.

## 1. P1 — does the pipeline re-run?

**Yes.** 3,415 feature rows loaded, 42 columns after pruning, 42 numeric features on 3,413 complete rows, PCA retained **19 components** at the 87% threshold.

## 2. P2 — does it select the shipped k?

Auto-selected **k = 3**; the shipped `CLUSTER_NAMES` mapping assumes k = 3. P2 **holds**.

| k | silhouette | Calinski-Harabasz | Davies-Bouldin | min cluster |
|---|---:|---:|---:|---:|
| 3 | 0.1398 | 527.2 | 2.1271 | 25.6% |
| 4 | 0.1352 | 446.7 | 2.1276 | 13.8% |
| 5 | 0.1172 | 386.9 | 2.2287 | 13.8% |
| 6 | 0.1203 | 349.7 | 2.1622 | 5.6% |
| 7 | 0.1096 | 320.2 | 2.1544 | 5.1% |
| 8 | 0.1054 | 296.6 | 2.1427 | 3.8% |

## 3. P3 / P4 — do the labels agree?

Dates present in both the stored file and this run: **3,413** (stored has 3,413; this run produced 3,413).

- Raw agreement, ids as written: **1.0000**
- Best-permutation agreement: **1.0000** (P3 target >= 0.99 — **met**)
- Best permutation (fresh -> stored): `{0: 0, 1: 1, 2: 2}` — identity: **True** (P4 holds)

Confusion matrix (rows stored, columns regenerated):

| stored \ fresh | 0 | 1 | 2 |
|---|---:|---:|---:|
| 0 | 1,575 | 0 | 0 |
| 1 | 0 | 964 | 0 |
| 2 | 0 | 0 | 874 |

## 4. Reading

The pipeline reproduces its own stored labels. Finding B's *practical* severity is lower than feared — the target definition is recoverable by re-running — but the defect stands: nothing in the repo persists the fitted objects, so this agreement is a property of today's data and library versions and is not guaranteed to survive either changing.

Either way the forward fix is the same and is unchanged by this result: `cluster_day_types.py` must persist its scaler, PCA and KMeans as plain arrays with a hash, so the next answer to this question is a file lookup rather than a re-run.

