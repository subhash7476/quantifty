# CSMP Gate (e) — Independent Review Request (for DeepSeek V4)

**Date:** 2026-07-11
**Reviewer requested:** DeepSeek V4 (independent Lead Reviewer)
**Why independent:** Gate (e) was implemented by GLM (session ended mid-work) and then **Claude fixed a verdict-flipping bug** (below). Claude therefore cannot be the Lead Reviewer — independence is spent, exactly as in gate (b) R5. This gate's CONTINUE verdict is what unlocks pre-registration and the first read of the **sealed 2023-01→2026-06 window**, so a wrong CONTINUE is expensive and irreversible. Re-derive everything; trust nothing in this document or in the report.

**Under review:**
- `scripts/csmp/triage_momentum.py`
- `docs/reports/CSMP_GATE_E_TRIAGE.md` (generated by the script)
- Prompt: `CSMP_IMPLEMENTATION_PROMPTS.md` §Prompt 5 (ISSUED, stop rule LOCKED: L=12, mean-IC floor 0.02, net-spread floor 0)

**Deliverable:** `docs/reports/CSMP_GATE_E_LEAD_REVIEW.md` — PASS / NOT PASSED with findings F1..Fn, each claim re-derived against the store, not taken from the report.

---

## The bug Claude fixed (verify the fix is correct AND complete)

`pairs` tuples are `(rank, sc, fwd, sym)` where **`rank` = `universe_membership.rank`, which is gate (c)'s 6-month-turnover (liquidity) rank — NOT momentum**, and `sc` = the 12-1 momentum score. The original code selected the quintile buckets with `sorted(pairs, key=lambda x: x[0])` — i.e., the **most-liquid** 40 names, not the highest-momentum 40. Claude changed both bucket sites (§4 spread and the §5 top-quintile portfolio) to `sorted(pairs, key=lambda x: x[1], reverse=True)` (momentum score, high->low).

- Confirm independently that `pairs[0]` is the turnover rank and `pairs[1]` is the 12-1 score, and that the corrected sort selects the highest-momentum quintile.
- The fix flipped the verdict **STOP -> CONTINUE**. Corroborating evidence Claude cites: momentum top-quintile two-way turnover rose from ~5% to **23.76%/month** (liquidity buckets barely rotate; momentum buckets should), universe churn 3.03%. Verify this turnover figure yourself — it is the strongest independent check that the bucket is now momentum-driven.
- **Look for other instances of the same class of error** and for any bug the fix did not address.

## Headline numbers to re-derive against the store (do not copy)

| Claim | Reported |
|---|---|
| Scored IC months | 131 (2012-01 -> 2022-11) |
| Mean rank IC | 0.0458 |
| Block-bootstrap 95% CI (L=12, 20000 reps, seed 20260711) | [0.0093, 0.0812] |
| Quintile gross spread (top-bottom) | +1.07%/mo, hit 60.31% |
| Top-quintile EW net (annualized) | 15.53% (gross 16.26%), turnover 23.76%/mo |
| Equal-weight universe net (annualized) | 9.16% (gross 9.25%), turnover 3.03%/mo |
| Net top-minus-universe spread | **+6.38%** |
| Verdict | CONTINUE |

## What could make CONTINUE spurious — scrutinize hardest

A wrong CONTINUE burns the held-out window, so weight the review toward anything that could **inflate the top-quintile spread**:

1. **Look-ahead / no-leak.** The score at `t` must use only prices <= `t-1m`; the forward return must be the realized `t -> t+1`. Confirm no future price enters the score. Confirm membership at `t` is gate (c)'s point-in-time set (no survivor list). Confirm the sealed-window fence: every input query asserts `MAX(trade_date) <= 2022-12-31`, and the last scored formation month is 2022-11 (forward -> 2022-12), so 2022-12's forward (2023-01, sealed) is excluded.
2. **Survivorship in the price panel.** `universe_membership` is point-in-time, but does the triage's entity-price series or forward-return step silently drop names that delist mid-hold? A name that leaves the top quintile by **delisting** (not by momentum decay) must still bear its realized (possibly bad) forward return, not vanish. Check how missing `px.get((ent, tp1))` is handled in both the IC pairs and the portfolio — a dropped delisting is a survivorship bias that flatters momentum.
3. **Entity-price dedup.** One price per `(entity, date)` via max-turnover (gate (c)'s active-ticker rule). Verify 0 residual `(entity, date)` duplicates and that rename continuity (`universe_eligibility`) is applied consistently to score, forward return, and membership join.
4. **Turnover/fee model symmetry.** "Bucket churn; equal-weight drift of continuing holdings not re-traded" is applied to **both** arms. Confirm it is genuinely symmetric and does not understate the top quintile's fees relative to the universe (the top rotates 8x more, so any asymmetry favors the universe — check the direction).
5. **Baseline fairness.** The equal-weight full-universe arm is the free baseline; confirm it holds all ~200 point-in-time members with the same forward-return and fee treatment.
6. **Stop rule applied mechanically** with the locked numbers (L=12, IC floor 0.02, spread floor 0) — no post-hoc change; a STOP would have been the legitimate D1-style outcome.

## Acceptance for this review

State PASS only if every headline number re-derives, the bucket key is confirmed to be momentum, no look-ahead/survivorship bias inflates the spread, the fence holds, and the report regenerates byte-identically. Otherwise NOT PASSED with named findings. If PASS, gate (e) CONTINUE is confirmed and pre-registration (Phase 1) may proceed — the first sanctioned read of the sealed window. If NOT PASSED, CSMP holds.
