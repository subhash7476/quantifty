# PTMS — Gann Stage-1 P-3 Build Note (code for the freeze; written, never run)

**Date:** 2026-09-19 · **Branch:** `research/ptms-price-time-market-structure`

**Status: IN PROGRESS.** P-3 implements `PTMS_GANN_STAGE1_FREEZE_DOCUMENT_DRAFT_2026-09-19.md` as
frozen, and nothing else.
- **The screen is not run.** Running it is the read the freeze exists to precede.
- Validation is by unit tests on **hand-built or randomly generated synthetic bars only**. No test
  reads a store.
- The entry point will refuse to execute unless **both** of these hold:
  1. the freeze digest recorded in checklist item 18 matches the committed freeze file;
  2. register row G-S1 is present.

  The pattern follows `scripts/signal_engine/ts_basis_daily/run_sealed.py`.

---

## 1. Module plan (`scripts/ptms/gann/`)

| Module | Freeze § | Status |
|---|---|---|
| `constants.py` | All frozen parameters (window, Z, P4, windows, B, seed, α, floors, calendar artifacts, ratio CA types) | **Done** |
| `calendar.py` | §6 ISO weeks, D_L, O_1 … O_5, cal(D_L) + 1 … + 7, OPEN-M | **Done** |
| `k3.py` | §3 K3 (memo §5 rows 1–13, OPEN-Q) and S9; vectorized over stock × panel columns | **Done** |
| `basis.py` | §7 ratio-adjusted basis from as-traded `equity_bhavcopy` and BONUS/SPLIT `adjustment_factors` at entity grain; as-traded close kept for D-PL | Next |
| `panel.py` | §6 PIT membership on D_L, entity resolution, BE included, TATAMTRDVR as its own stock, no `universe_eligibility` filter; G-7 events read from the frozen CSV (`g7_event = True`), never re-derived | Next |
| `outcome.py` | §4 O-R10: GF-1/GF-4T/R8 (RR-4/5/6) and GF-10 (OPEN-12, OPEN-K(b)), kept as separate code paths so the asymmetries cannot merge | Next |
| `gf1.py`, `gf4.py` | §2.2, §2.3 scores; placebo families generated by rule (§9) | Next |
| `gf10.py` | §2.4 = v0.8 §1–§3.12, DB profile; hard-fails if any observation is dated ≥ 2023-01-02 | Next |
| `stats.py` | §10 T_c with OPEN-P (drop and count), p_sur, p_plac, G-4 contrast p, effect size | Next |
| `surrogate.py` | §8 synchronized stationary bootstrap, mean block 20, G-5 nearest bar in block, seed-42 stream layout; RR-8 masks | Next |
| `robustness.py` | §12 variants and diagnostics | Next |
| `run_screen.py` | Guarded entry point; size check before unblinding; script-filled report per §13 | Next |

## 2. Findings from reading code and metadata (no price rows read)

| # | Finding | Consequence for P-3 |
|---|---|---|
| F-1 | `equity_bhavcopy_adjusted` applies **SPECIAL_DIVIDEND** factors as well as BONUS and SPLIT (`build_adjusted_view`, `scripts/csmp/ingest_corporate_actions.py`) | **Not usable as the Stage-1 basis.** Freeze §7 limits the ratio basis to bonus, split and consolidation, and makes special dividends G-7 exclusions. `basis.py` builds its own series from BONUS/SPLIT factors only. **Derived:** a single multiplicative basis equals the as-of-*t* basis up to a constant on every price ≤ D, so every comparison and every magnitude comparison is unchanged (v0.8 §3.10, basis) |
| F-2 | `trading_calendar` (2011-03-25 → 2022-12-30: 2,919 rows) still lists **2012-11-11** (source `equity_store`) and **2016-04-19** (source `unresolved`) | 𝒟 = `trading_calendar` minus `NON_SESSIONS` (freeze §6). The calendar dates were read; no price was |
| F-3 | Weekend sessions in the window: Saturdays 2012-01-07, 2012-03-03, 2012-04-28, 2012-09-08, 2013-05-11, 2014-03-22, 2015-02-28, 2020-02-01, 2020-11-14; Sundays **2013-11-03, 2016-10-30, 2019-10-27** (plus the 2012-11-11 artifact) | All are ordinary members of 𝒟. The three Sunday Muhurat sessions are each the last session of their ISO week (CAL-1). Freeze §6 names only 2016-10-30; the rule covers all three |
| F-4 | The screen is DB-only, so a session's **date** is all P-3 needs, never its intraday window | The §14 "session-window source" obligation is met by `trading_calendar` for the date. No intraday time enters any DB computation |

## 3. Implementation readings for operator confirmation (IR)

These are places where the frozen text is silent at the level of code. P-3 implements the reading
shown, and the tests pin it. None is a new scientific choice claimed as frozen. The operator should
confirm them, or rule otherwise, **before P-5**, and they are then written into the freeze document.

| ID | Where | Reading implemented | Alternative | Why it matters |
|---|---|---|---|---|
| **IR-1** | K3 swing date on **equal extremes** within one line (memo §5 row 2 says "date = day of that high") | **First occurrence**, as v0.8 OPEN-4.5 locks for GF-10 | Last occurrence | Moves GF-4T/R8 window dates, O-R10 reference dates and G-7 span starts. GF-10 is already locked |
| **IR-3** | GF-1: does a high **equal** to the running highest high "replace" the anchor and restart its count (§2.2, R-3)? Mirror for lows | **No.** Only a strictly higher high (strictly lower low) replaces the anchor, so the anchor date is the first occurrence | An equal value restarts the count | Moves GF-1 point dates whenever a stock equals its to-date extreme |
| **IR-2** | Which sessions are "while UP" for the swing high (memo §5 row 2) | The sessions whose **post-close state** is UP: from the switch session that set UP (inclusive) to the down-switch session (exclusive). The down-switch session belongs to DOWN. Mirror for lows | Include the switching session in the old line | Only an **outside day** inside a down run can set a new high, so it changes the swing high only in that case. Test `test_inside_day_breaks_down_run_and_outside_day_extends_it` pins it |

## 4. Compute budget (engineering, not a definition)

- G-9a requires every one of the 200 pseudo-real panels to be tested with its own B = 1999
  surrogates. That is about **400,000 panel evaluations**, plus B = 1999 for the real test. One set of
  panels can serve all three constructs.
- Timing: vectorized K3 takes **≈ 0.21 s per 180-stock panel** on one core (synthetic panels,
  2,919 sessions), so **≈ 23 h** for the size check's K3 alone.
- Scoring, outcomes and the GF-10 ledger will add to that. The machine has 4 cores and no numba.
- **Expectation:** a size-check run of days, not hours. This is recorded so that the operator is not
  surprised at run time. It does not change G-9a.

## 5. Tests

`tests/ptms/test_gann_k3.py` passes 10 of 10:
- initialization;
- the first-occurrence swing high;
- equal-low, inside-day and outside-day handling;
- the OPEN-Q skip;
- S9 BULL;
- ISO week with a Sunday Muhurat;
- OPEN-M;
- a scalar transcription of memo §5 matched against the vectorized K3 on 25 random columns with ties
  and gaps.

**NO STORE PRICE, OUTCOME OR COUNT READ. NOTHING RUN ON MARKET DATA.**
